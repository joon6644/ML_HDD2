# project_A/venv 에 이미 설치된 것: pandas, numpy, pyarrow, scikit-learn,
# xgboost, lightgbm, torch, duckdb, scipy, matplotlib
#
# project_B 가 추가로 필요한 것은 PyYAML 하나다.
#   pip install pyyaml
#
# 전체 목록 (새 환경을 만들 경우):
pandas>=2.2
numpy>=1.26
pyarrow>=15
duckdb>=1.0
PyYAML>=6.0
scikit-learn>=1.4
scipy>=1.11
xgboost>=2.0
lightgbm>=4.0
torch>=2.2
matplotlib>=3.8
pytest>=8.0
