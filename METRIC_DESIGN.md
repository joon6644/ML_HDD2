# HDD 운영 평가 지표 설계 노트

논문 *"SMART 기반 HDD 고장 예측 모델의 운영 환경 기반 성능 평가"*의 3장 지표 설계에 대한 검토 기록.
각 결론에 그 근거가 된 수치를 함께 남긴다. 근거를 지우면 결론도 재현되지 않는다.

작성 시점 기준 실험 결과: `results/experiments.db`, seed 42~62, 3 데이터셋 × 4 모델.

---

## 0. 결론 요약

**채택**

| 지표 | 수식 | 분모 모집단 |
| --- | --- | --- |
| Disk-level Recall | `O / (O + E + M)` | 고장 관측 HDD |
| Disk-level Precision | `O / (O + E + CE)` | 알람 발생 HDD |
| Disk-level FAR | `CE / (CE + CN)` | 우측 검열 HDD |
| Median Lead Time | `median(LT_d), d ∈ O ∪ E` | 알람 발생한 고장 HDD |
| (보조) On-time 비율 | `O / (O + E)` | 알람 발생한 고장 HDD |

임곗값 선택 제약: **Disk-level FAR ≤ 1%** (행 단위 FAR ≤ 1%와 대응)

**폐기**

- `EAP = (E + CE) / N_all` — 제약에서 내림. 서술 지표로도 불필요
- 두 모집단 FAR의 가중 결합 / 기하평균 — 아래 3절 참조
- 새 약어 `OAP` / `ODR` / `EAP` — 표준 명칭으로 대체

**변경 비용**: 재학습 불필요. `evaluator.py`가 이미 위 세 지표를 계산 중이며, 바꿀 것은 임곗값 탐색의 제약 하나뿐이다.

---

## 1. 5개 결과 범주

| HDD 상태 | 최초 Alarm | 조건 | 판정 | 기호 |
| --- | --- | --- | --- | --- |
| 고장 관측 | 있음 | `LT_d ≤ H` | On-time | O |
| 고장 관측 | 있음 | `LT_d > H` | Early | E |
| 고장 관측 | 없음 | — | Missed | M |
| 우측 검열 | 있음 (`T_d^eval` 내) | — | Censored Early | CE |
| 우측 검열 | 없음 | — | Censored No Alarm | CN |

`H = 30일`, `T_d^eval = {t ∈ T_d : t ≤ t_end,d − H}`

### 검증된 성질

- **완전 분할**: 모든 디스크가 정확히 하나에 배정. 5개 카운트 합 = 전체 디스크 수
- **정보 누수 없음**: 판정에 시간 순 온라인 추론의 최초 알람과 관측된 고장 기록만 사용
- **`failure_date` 정의 명확**: 코드에 "RUL==0 행이 없으면 마지막 관측일 사용"이라는 대체 경로가 있으나, **3개 데이터셋 test 전부 해당 0건**. 실제로는 항상 명확히 정의된 고장일을 사용
- **음수 Lead Time 불가능**: 고장일 이후 관측 행 0건, `RUL < 0` 행 0건, 비시간순 segment 0건 (3개 데이터셋 test 전수 확인)
- **Late 범주가 구조적으로 불가능**: HDD는 고장 시 관측이 종료되므로 사후 알람이 존재할 수 없음. 위 검증이 이를 뒷받침

### 구조의 성격

곱(cross)이 아니라 **층화(stratification)**다. 3 × 2 = 6이 아닌 5개인 이유는:

> 검열 상태가 **어떤 시간적 결과를 식별 가능하게 하는지**를 결정한다.
> 고장 관측 → `LT_d` 계산 가능 → O / E / M 식별 가능
> 우측 검열 → `LT_d` 계산 불가, 단 `T_d^eval` 내 알람은 On-time이 될 수 없음이 확정 → CE / CN

논문에서는 **"event-level 시간 분류 + 우측 검열 상태 층화의 결합"**으로 서술한다. 무에서 발명한 것이 아니라 두 흐름을 잇는 위치로 읽히게 하는 것이 목적.

### 선례

`notion.md` 3.2 표에 이미 정리된 문헌이 근거가 된다. 새로 인용을 찾을 필요 없음.

- **MEWS (2021)** — 경보를 Early/On-time/Late/Missed로 분류 후 민감도·오경보율 계산
- **A framework to characterize the performance of early warning index alarm systems** — False/Early/On-time/Late/Missed 분류 후 PPV·Sensitivity·FPR 계산
- **Hypoglycemia Prediction** — 45분 이내 경보만 TP, 그보다 이른 경보는 **실제 사건이 발생해도 FP** 처리 후 Sensitivity·Specificity·FAR·Time to Detection 계산

두 가지가 도출된다.

1. **Early를 벌점으로 두는 발상에는 선례가 있다** (Hypoglycemia)
2. **그러나 이 논문들은 모두 분류 후 모집단별 표준 지표를 계산한다.** 두 모집단을 하나의 비율로 합치지 않는다 ← EAP가 선례를 벗어난 지점

Lead Time 계열도 HDD 문헌에 선례가 있다: StreamDFP "Days in advance", LightGBM+CID "DPF", DFPoLD "평균 DPF". 발명이 아니라 **기존 지표를 최초 알람 기준으로 정의한 것**.

---

## 2. EAP를 제약에서 내리는 이유

기존 정의: `EAP = (N_E + N_CE) / N_All`

### 결함 1 — 분자와 분모의 모집단이 어긋난다

`N_E`는 고장 관측 HDD에서만, `N_CE`는 검열 HDD에서만 나온다. 그런데 분모는 둘의 합집합이다. 어느 모집단의 비율도 아니므로 해석이 불가능하다.

**원칙**: 분모는 분자를 길어 올린 모집단과 일치해야 한다.
Precision(`O/(O+E+CE)`)이 두 모집단에 걸쳐도 정당한 이유는, 그 분모가 클래스가 아니라 **"알람이 발생한 디스크"라는 예측 기반 단일 집합**이기 때문이다.

### 결함 2 — 분자의 대부분이 오탐이 아니다

EAP 분자 중 `N_Early`(고장을 맞혔으나 일찍 울린 경우)가 차지하는 비중:

| 데이터셋 | Early 비중 | N_Early (평균) | N_CE (평균) |
| --- | --- | --- | --- |
| HGST | **77.6%** | 8.98 | 2.89 |
| ST12000 | 48.7% | 18.94 | 20.00 |
| TOSHIBA | 68.6% | 25.13 | 11.94 |

모델별로는 48~86%. 즉 EAP는 오탐률이 아니라 **주로 조기 탐지 페널티**다.

### 결함 3 — 행 단위 FAR과 대응하지 않는다

`notion.md` 4.2는 "행 단위 FAR ≤ 1%"와 "운영 EAP ≤ 1%"를 대응 제약으로 제시한다. 그러나 진짜 대응물인 검열 전용 FAR은 전 조건에서 **0.17~0.56%**로 이미 1% 아래다. 엄격도가 전혀 다르다.

→ **RQ2가 교란된다.** "Row 최적화 vs 운영 최적화의 임곗값 차이"를 보는 실험인데, 그 차이의 상당 부분이 평가 단위 차이가 아니라 **제약 정의 차이**에서 발생한다.

### 결함 4 — 조기 탐지를 벌주는 방향으로 최적화된다

`N_Early`가 분자를 지배하므로, EAP를 조이면 모델은 오탐을 줄이는 대신 **알람을 고장에 더 가깝게 미룬다**. Recall도 Early를 분자에서 제외하므로 같은 방향.

실제 결과: Median Lead Time 4분위 **12.5 / 20 / 37일**, 189행 중 68행이 15일 이하.
논문 1.2와 3.1은 Lead Time을 가치로 놓는데 프로토콜은 그것을 비용으로 계산하는 모순.

### 결함 5 — 데이터셋 간 비교가 깨진다

`N_Early`와 `N_CE`의 기여도가 모집단 구성비에 따라 달라진다. 검열 비중: HGST 85.8% / ST12000 94.2% / TOSHIBA 94.7%. 동일한 모델 거동이 데이터셋에 따라 다른 EAP를 낸다.

### 파생 결과 — HGST/LGBM 제약 불만족은 상당 부분 정의의 산물

HGST+LGBM은 검증셋에서 그리드 전체를 훑어도 EAP ≤ 1%를 만족하지 못한다(최소 달성 EAP 1.67~2.73%, seed 13개 전부). 그러나 같은 데이터셋의 검열 전용 FAR은 0.17~0.45%다. 즉 오탐이 많아서가 아니라 **`N_Early`가 분자에 있어서** 불만족이 된 것.

(별도 원인도 있음: LGBM의 확률이 정확히 1.0에 포화되어 그리드 상한 0.999로는 잠재울 수 없는 디스크가 1135대 중 약 30대 존재. 이는 불균형 처리나 서브샘플링으로 해결되지 않음 — 실측 확인.)

---

## 3. 결합 지표를 만들지 않는 이유

### 가중 결합 = EAP (정확히 동일)

모집단 크기로 가중 평균하면:

```
w_c · FAR_cens + w_f · Early_rate
  = (CE+CN)/N · CE/(CE+CN)  +  (O+E+M)/N · E/(O+E+M)
  = CE/N + E/N
  = (E + CE)/N
  = EAP
```

분모가 약분되며 EAP가 그대로 나온다. **EAP가 그럴듯해 보였던 이유가 이것이고**, 동시에 가중치가 데이터셋의 검열 비중이라는 결함도 이것으로 설명된다.

→ 가중 결합으로 돌아가면 창작 지표를 다시 도입하면서 데이터셋 간 비교 불가 문제까지 되살린다.

### 기하평균은 더 나쁘다

- **영점 붕괴**: 둘 중 하나가 0이면 나머지와 무관하게 0. 임곗값을 올리면 두 값이 0이 되는 구간이 넓어 다수의 임곗값이 동점 → 선택 불가
- **용도 불일치**: G-mean이 민감도×특이도에 쓰이는 것은 둘 다 **높일 대상**이기 때문. 여기 둘은 **낮출 대상**이라 균형이 아니라 각각의 상한이 필요
- 결과값의 운영적 해석이 없음

### 통합 수치가 필요하면 이미 있다

```
1 − Precision = (E + CE) / (O + E + CE)
```

분자가 EAP와 **같고**, 분모만 "알람 발생 HDD"라는 명확한 집합으로 정규화된 형태.
해석: **발생한 알람 중 On-time으로 판정되지 않은 비율.**

> 주의: "EAP와 동일하다"고 쓰면 안 된다. 분모가 다르다.

---

## 4. 왜 5상태를 2×2로 접지 않는가

Early를 어디에 넣어도 깨진다.

| 배치 | 결과 |
| --- | --- |
| FN | 완전 분할은 되지만 Precision이 깨짐. Early 디스크는 실제로 알람을 냈는데 "알람 발생 디스크" 집합에서 빠짐 |
| FP | FP가 음성 모집단에서만 나와야 한다는 전제가 깨짐. Early는 양성(고장) 디스크에서 발생 |
| TP | Recall이 무의미해짐. 200일 전 알람도 탐지 성공이 됨 |

디스크 단위 결과는 **(검열 여부 × 알람 유무 × 시점 적절성)** 세 축으로 결정되는데 2×2는 네 칸뿐이다. 억지 매핑 대신 각 지표의 분모를 모집단으로 고정하고 **행 단위와의 대응 관계를 표로 명시**한다.

### 행 단위 대응표

| 개념 | Row-level | Disk-level | 분모 모집단 |
| --- | --- | --- | --- |
| 탐지율 | `Recall = TP/(TP+FN)` | `O/(O+E+M)` | 고장 관측 HDD |
| 오탐율 | `FAR = FP/N_neg` | `CE/(CE+CN)` | 우측 검열 HDD |
| 정밀도 | `Precision = TP/(TP+FP)` | `O/(O+E+CE)` | 알람 발생 HDD |
| — | 대응 없음 | `O/(O+E)` | 알람 발생한 고장 HDD |
| — | 대응 없음 | Median Lead Time | 알람 발생한 고장 HDD |

### Early의 비용은 사라지지 않는다

- **Recall 분모** — 적시 탐지 실패로 Recall을 깎음
- **Precision 분모** — 불필요한 정비 행위로 Precision을 깎음
- **FAR** — 해당 모집단이 아니므로 보지 않음 (의도적)

같은 사건의 이중 계산이 아니라 **서로 다른 두 비용**이 각각 반영되는 것.

행 단위와의 대응도 여기서 강해진다: 레이블이 `(RUL ≤ H) & (censored == 0)`이므로 **행 단위 평가도 이미 너무 이른 예측을 FP로 센다.** "too early = 오답"은 새 규칙이 아니라 행 단위 정의를 개체 단위로 옮긴 것이다.

---

## 5. FAR을 검열 모집단에서만 계산하는 이유

### 논문에 쓸 1순위 근거 — 운영적 정의

> FAR은 "고장이 관측되지 않은 HDD 중 불필요한 알람이 발생한 정도"를 측정하도록 정의한다. 고장 관측 HDD의 Early Alarm은 실제 고장에 대한 시간적 부적절성을 의미하므로 FAR의 negative population에 포함시키지 않고, Recall과 Precision을 통해 각각 적시 탐지 실패와 부적절한 알람으로 반영한다.

형식 논증(모집단 분할)을 앞세우면 형식 반론을 부른다. 운영적 정의를 먼저 두고 분할 논리는 보조로.

### 보조 근거 — 클래스 분모는 서로소여야 한다

`Recall = TP/P`와 `FPR = FP/N`은 `P ∩ N = ∅`을 전제한다. 고장 HDD를 FAR 분모에 넣으면 Recall 분모에서 빼야 한다.

> Precision은 반례가 아니다. Precision의 분모는 정답 클래스가 아니라 **예측 결과로 정의된 집합**이므로 두 클래스에 걸쳐도 무방하다.

### 보조 근거 — 행 단위 음성의 대부분이 이미 검열 디스크

| 데이터셋 | 음성 행 중 검열 디스크 기원 | 고장 디스크 기원 |
| --- | --- | --- |
| HGST | 89.35% | 10.65% |
| ST12000 | 96.01% | 3.99% |
| TOSHIBA | 96.80% | 3.20% |

검열로 한정하는 것은 임의적 축소가 아니라 원래도 거의 같은 모집단이었던 것을 개체 단위로 표현한 것.

> 주의: **개념적 대응**만 주장할 것. 분모와 단위가 다르므로 **수치적 동일성**을 주장하면 안 된다.

### 부수 성질 — 커버리지

`CN`은 FAR에만 등장한다. FAR을 고장 모집단으로 옮기면 CN이 어느 지표에도 나타나지 않고, CN은 전체 평가 대상의 **85~95%**다. 그러면 "정상 디스크를 건드리지 않았다"가 성능으로 인정받을 수 없고, 모든 디스크에 알람을 뿌리는 모델이 만점을 받는 실패 양상이 생긴다.

> **인과 순서 주의**: "검열 데이터를 쓰려고 FAR을 거기 배치했다"로 쓰면 지표를 데이터에 맞춘 것으로 읽혀 약해진다.
> 올바른 순서: 운영적 정의 → 그 정의상 분모가 검열 HDD → **결과적으로** 5개 범주가 모두 사용됨.
> 논문에 쓸 수 있는 형태: *"제안한 네 지표는 5개 결과 범주를 모두 사용하며, 어떤 지표에도 반영되지 않는 HDD는 존재하지 않는다."* 분류가 과잉 설계되지 않았다는 증거가 된다.

---

## 6. "검열 = 음성" 매핑의 정당성

**우려**: 어차피 고장날 HDD인데 고장이 관측되지 않았다는 이유로 음성으로 두어도 되는가.

**답 1 — 레이블이 예측 기간에 상대적이다.**
양성 클래스는 "언젠가 고장난다"가 아니라 **"H일 이내에 고장난다"**이다. 모든 HDD가 결국 고장난다는 사실은 이 명제의 참·거짓과 무관하다. 알람 이후 H일간 고장이 관측되지 않았다면 그 예측은 관측으로 반증된 것.

**답 2 — 행 단위 레이블과 동일한 구성이다.**
학습 레이블이 `(RUL ≤ H) & (censored == 0)`이므로 행 단위에서도 검열 디스크의 모든 행은 음성이다. 디스크 단위 음성은 **모델이 학습된 바로 그 레이블을 개체 단위로 옮긴 것**이다.

→ 이 우려가 성립한다면 행 단위 평가에도 똑같이 성립하며, 그것은 기존 HDD 문헌 전체에 해당한다. 제안 평가만 차별적으로 무너뜨리지 못한다.

**답 3 — 오히려 더 엄격하다.**
`T_d^eval` 트리밍으로 검증 불가능한 마지막 H일을 제거하므로, CE로 집계되는 알람은 **이후 최소 H일간 고장 미관측이 관측적으로 보장**된다. 기존 연구는 검열 디스크를 그냥 정상으로 취급하거나 버린다.

**잔여 위험**: 알람 31일 후 고장난 검열 디스크도 오탐으로 센다. 단 이는 Early/On-time 경계와 **동일한 성격의 하드 컷오프**라 일관되며, H 민감도 분석이 둘을 함께 방어한다.

---

## 7. 강건성 검증 결과

XGB, seed 42, 운영 임곗값 기준 실측.

### 검열 비율 → 강건함 (확인됨)

검열 모집단을 부분추출했을 때:

| 데이터셋 | 전체 | 75% | 50% | 25% |
| --- | --- | --- | --- | --- |
| HGST | 0.512% | 0.502% | 0.495% | 0.533% |
| ST12000 | 0.383% | 0.383% | 0.382% | 0.392% |
| TOSHIBA | 0.134% | 0.137% | 0.128% | 0.137% |

평균 불변, 신뢰구간만 축소. **분모가 검열 모집단으로 고정되어 비율이 구성비에 불변**임이 실증됨. EAP였다면 가중치가 이동해 값 자체가 변했을 것 — 이것이 분리 설계의 실질적 이득.

### 관측 길이 → 판정 불가 (검정력 부족)

관측일수 4분위별 FAR:

| 데이터셋 | Q1 / Q2 / Q3 / Q4 | 알람 발생 검열 디스크 **총 수** |
| --- | --- | --- |
| HGST | 1.049 / 0.337 / 0.521 / 0.000% | **5대** |
| ST12000 | 0.218 / 0.435 / 0.651 / 0.223% | **14대** |
| TOSHIBA | 0.000 / 0.108 / 0.196 / 0.238% | **5대** |

TOSHIBA만 단조 증가로 보이나 사분위당 0, 1, 2, 2대. 패턴이라 할 수 없음. HGST는 감소 방향.

또한 **데이터셋 내부의 관측 길이 산포가 작다**. HGST 4분위 경계가 2466 / 2494 / 2558일로 전체 폭이 중앙값의 8% 수준. 대부분의 검열 디스크가 데이터셋 종료일까지 생존하기 때문. 탐지할 효과 자체가 거의 없다.

**결론**: 길이 정규화(디스크-년당 알람 수)를 도입하지 않는다. 필요성이 입증되지 않았는데 지표를 추가하는 것은 창작 최소화 기조와 어긋난다. 대신 **검열 디스크 관측일수 분포를 논문에 싣고, 데이터셋 간 FAR 절대값 직접 비교는 자제**한다. RQ1·RQ2는 동일 데이터셋 내 비교라 영향 없음.

### 실제로 부각된 문제 — FAR의 소수 사건 추정

FAR이 **5~14대**의 디스크로 추정되고 있다. HGST를 25% 부분추출하면 95% 범위가 0.000~1.240%로, 제약선 1%가 구간 안에 통째로 들어간다. seed마다 임곗값이 크게 흔들리는 현상의 상당 부분이 여기서 온다.

지표 정의의 결함이 아니라 **데이터의 구조적 한계**다(오탐률을 1% 아래로 누르면 필연적으로 사건 수가 적어짐). 대응:

- 결과표에 FAR과 함께 **`N_CE` / `N_CN` 원자료 병기**. "0.512%"만으로는 정밀도를 판단할 수 없으나 "5/977"이면 즉시 알 수 있음
- **다중 seed 집계가 완화 장치**임을 5.3에서 명시. 21개 seed면 사건 수가 100대 규모로 누적됨
- 가능하면 FAR 신뢰구간(Wilson 등) 병기

---

## 8. 제출 전 남은 과제

우선순위 순.

### ① FAR 제약으로 임곗값 재탐색 → 결론 유지 여부 확인 (최우선)

현재 189행은 전부 **EAP ≤ 1% 제약으로 고른 임곗값**의 결과다. FAR ≤ 1%로 바꾸면 임곗값이 달라지고 5.1~5.3의 모든 수치가 바뀐다. 핵심 주장(평가 단위에 따른 순위 변화, Top-1 agreement, Spearman)이 새 제약에서도 유지되는지 **아직 확인되지 않았다.** 이것이 확인되지 않으면 지표 설계가 아무리 정교해도 논문이 성립하지 않는다.

재학습 불필요. 저장된 예측으로 재탐색만.

#### 축소판 미리보기 결과 (seed 42, XGB·LSTM)

| 데이터셋 | 모델 | 규칙 | 임곗값 | Recall | Precision | 테스트 FAR% | Median LT |
| --- | --- | --- | --- | --- | --- | --- | --- |
| HGST | XGB | EAP (구) | 0.477 | 0.049 | 0.381 | 0.51 | 28 |
| HGST | XGB | FAR (신) | 0.286 | 0.086 | 0.326 | 0.72 | 57 |
| HGST | LSTM | EAP (구) | 0.327 | 0.099 | 0.533 | 0.41 | 19.5 |
| HGST | LSTM | FAR (신) | 0.101 | 0.130 | 0.253 | **1.84** | 77 |
| ST12000 | XGB | EAP (구) | 0.251 | 0.182 | 0.603 | 0.38 | 4 |
| ST12000 | XGB | FAR (신) | 0.089 | 0.240 | 0.419 | 0.96 | 15 |
| ST12000 | LSTM | FAR (신) | 0.287 | 0.160 | 0.308 | **1.23** | 30 |
| TOSHIBA | XGB | EAP (구) | 0.905 | 0.086 | 0.391 | 0.13 | 49 |
| TOSHIBA | XGB | FAR (신) | 0.063 | 0.177 | 0.306 | 0.89 | 41.5 |
| TOSHIBA | LSTM | FAR (신) | 0.178 | 0.144 | 0.275 | 0.89 | 52 |

**방향**: FAR 제약이 EAP보다 느슨하므로 임곗값이 크게 낮아지고, Recall과 Median Lead Time이 함께 오르며 Precision이 내려간다. Lead Time 상승은 논문 서사와 부합.

**주의 1 — 테스트 FAR이 제약선을 넘는다.** 검증셋에서 제약을 걸고 테스트에서 보고하므로 초과 자체는 정상이며 행 단위도 동일하다(테스트 Row FAR 최대 1.22%). 문제는 폭이다(최대 1.84%). 원인은 7절의 소수 사건 추정 — 검증셋에서 제약이 걸리는 사건이 수십 건뿐이라 임곗값이 검증셋에 과적합된다. EAP 시절에는 제약이 훨씬 빡빡해 여유가 있었으나 이제 경계에 밀착한다.
→ 대응: **검증 FAR과 테스트 FAR을 나란히 보고**하고 소수 사건 한계를 명시한다. 검증 목표를 0.8% 등으로 낮춰 여유를 두는 방법도 있으나, 임의 값이 하나 늘어나므로 권하지 않는다.

**주의 2 — 모델 순위가 뒤집힌 사례가 있다.** TOSHIBA에서 `LSTM > XGB`(구) → `XGB > LSTM`(신).
제약 정의가 순위를 바꾼다는 것은 2절 결함 3의 교란이 실재했다는 증거이지만, 동시에 **순위 자체의 불안정성**을 뜻하기도 한다. 5.3의 "평가 단위에 따른 순위 변화" 주장이 "순위는 원래 흔들린다"는 반론에 노출된다.
→ 단 모델 2개·seed 1개의 관찰이므로 결론이 아니다. 4모델 × 전 seed 재탐색 후 ②번(지표 강건성)에서 다뤄야 한다.

### ② 지표 강건성 논증

"결론이 당신이 만든 지표의 산물 아니냐"가 창작 지표에 대한 표준 공격. **Precision·Recall·FAR 세 지표 모두에서** 평가 단위 간 괴리가 나타남을 보이면 무력화된다. 데이터는 이미 있음(189행에 두 임곗값에서의 disk-level precision/recall/FAR 저장됨).

참고로 Row 기준 임곗값을 운영 평가에 적용했을 때 검열 FAR:

| 데이터셋 | Row 기준 | 운영 기준 | 배율 |
| --- | --- | --- | --- |
| HGST | 6.99% | 0.29% | 23.8× |
| ST12000 | 5.65% | 0.55% | 10.3× |
| TOSHIBA | 7.24% | 0.32% | 22.6× |

### ③ H = 30일의 근거 + 민감도 분석

`notion.md` 3.2는 H를 선언만 하고 근거가 없다. 그런데 5개 범주 중 2개의 경계와 주요 지표 2개가 이 값에 걸려 있다.

게다가 데이터가 경계에 몰려 있다: Median Lead Time 4분위 12.5 / 20 / **37**일, 189행 중 **68행이 Median LT > 30**. H를 조금만 옮겨도 상당한 질량이 On-time ↔ Early를 넘나든다.

비교 대상인 Hypoglycemia 논문은 45분 컷오프에 **개입 소요 시간**이라는 임상적 근거가 있었다. 둘 중 하나가 필요:
- 운영적 근거 (데이터센터 교체·조달 리드타임 등, 인용 포함)
- **H 민감도 분석** (7/15/30/60일)로 핵심 결론이 H에 견고함을 입증 ← 더 안전

### ④ Late 범주 부재의 설명 (한 문단)

인용 문헌은 모두 Late를 두는데 여기만 없다. 근거는 확실하다(고장 시 관측 종료 → 사후 알람 구조적 불가능, 3개 데이터셋 0건 확인). 쓰지 않으면 **누락**으로 읽힌다.

### ⑤ 비정보적 검열 가정 명시 (한 문단)

Backblaze에서 드라이브는 고장 외 사유로도 fleet을 떠난다. 그 이탈이 열화와 상관되어 있다면 CE로 분류된 알람 중 일부는 오탐이 아니다. 해결하라는 것이 아니라 **명시하라**는 것: *"본 연구는 우측 검열이 고장 위험과 독립이라고 가정한다"* + 가정이 깨질 때의 방향성(FAR 과대추정). 검열 데이터를 다루는 논문의 표준 심사 질문.

### ⑥ 최초 Alarm 단일 사건 축약의 정당화

하루치 단발 알람과 30일 지속 알람이 동일하게 집계된다. 실제 모니터링은 지속성 조건으로 debounce하므로 "왜 first alarm이지 first *sustained* alarm이 아닌가"를 물을 수 있다. `analysis/lead_time_analysis/run_hgst_alarm_escalation_policy_2x2.py`로 이미 탐색해 두었으므로 민감도 분석으로 본문에 끌어올린다.

### ⑦ 5개 카운트 원자료 표

혼동행렬을 싣는 것과 같은 이유. 독자가 원하는 지표를 직접 계산할 수 있고, 새 분류를 제안하는 논문일수록 원자료 공개가 신뢰를 만든다. `N_On_time` ~ `N_Censored_No_Alarm` 5개 컬럼이 이미 DB에 있음.

---

## 9. 주장할 때 조심할 표현

| 쓰면 안 되는 표현 | 대신 |
| --- | --- |
| "이런 분류를 시도한 논문은 없다" | "본 연구가 조사한 범위에서는 확인하지 못하였다" |
| "행 단위 FAR과 디스크 FAR이 동일하다" | "개념적으로 대응한다" (수치적 동일성 아님) |
| "1 − Precision은 EAP와 동일하다" | "분자는 같고 분모가 다르다" |
| "알람 단위 PPV = Disk Precision" | "HDD별 최초 알람만을 평가 대상으로 하므로 동일한 집계 결과를 갖는다" (조건부) |
| "검열 데이터를 쓰기 위해 FAR을 배치했다" | 운영적 정의 → 분모 결정 → 결과적 커버리지 (인과 순서) |
| "고장 HDD에서는 FAR을 계산할 수 없다" | 계산은 가능하나 그것은 FAR이 아니라 Early rate이며, Recall·Precision에 반영됨 |
| "모델 간 공정한 비교를 위해 동일한 학습 조건" | 실제 하이퍼파라미터 표를 명시 (XGB와 LGBM의 서브샘플링 설정이 달랐던 이력 있음) |

---

## 10. 구현 상태

`experiments/evaluator.py`에 세 지표가 이미 구현되어 있다.

```python
precision = hits / (hits + false_alarms)   # false_alarms = fp_early + fp_cens  →  O/(O+E+CE)
recall    = odr                            #                                      O/(O+E+M)
far       = fp_cens / censored_disks       #                                      CE/(CE+CN)
```

바꿀 것은 **임곗값 탐색의 제약을 EAP에서 FAR로 교체**하는 것 하나.
5개 카운트가 DB에 저장되어 있으므로 기존 189행의 지표는 즉시 재계산 가능하며, 임곗값 자체만 저장된 예측으로 재탐색하면 된다. **재학습 불필요.**

---

## 11. 핵심 수치 레퍼런스

### 데이터셋 구성 (test split)

| 데이터셋 | 고장 관측 | 우측 검열 | 검열 비중 | 검열 관측일수 중앙값 |
| --- | --- | --- | --- | --- |
| HGST_20HUH721212ALN604 | 162 | 977 | 85.8% | 2,494일 |
| ST12000NM0007 | 225 | 3,656 | 94.2% | 952일 |
| TOSHIBA_20MG07ACA14TA | 209 | 3,728 | 94.7% | 1,917일 |

### 무결성 확인 (3개 데이터셋 test 전수)

| 항목 | 결과 |
| --- | --- |
| feature 컬럼 NaN | 0 |
| `RUL < 0` 행 | 0 |
| 고장일 이후 관측 행 | 0 |
| 비시간순 (segment, date) | 0 |
| `RUL == 0` 행이 없는 고장 디스크 | 0 |

### 운영 임곗값에서의 지표 (전 seed 평균)

| 데이터셋 | EAP | 검열 FAR | Recall(ODR) | Median LT |
| --- | --- | --- | --- | --- |
| HGST | 1.04% | 0.30% | 0.08 | 24.8일 |
| ST12000 | 1.00% | 0.55% | 0.16 | 13.6일 |
| TOSHIBA | 0.94% | 0.32% | 0.10 | 40.6일 |

EAP가 1.0 근처인 것은 그것이 **구속 제약**이기 때문. 검열 FAR은 그 상태에서 이미 0.3~0.55%.
