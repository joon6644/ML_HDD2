정보기술학회 게재, KCI 등재 목표

kci 평가기준 

- 데이터셋
    
    https://drive.google.com/drive/folders/18TOKVO7WfFPBD3UTUz4EEedY6UjSC2ct?usp=sharing
    
- 가이드
    
    
    | 용어 | 뜻 |
    | --- | --- |
    | 행 단위 평가 (Row-level evaluation) | 각 관측 행을 독립적인 분류 대상으로 평가하는 방식 |
    | 운영 환경 기반 평가 (Operational evaluation) | HDD의 시간 순차적 관측 과정을 유지한 채, 최초 Alarm과 실제 고장 또는 관측 종료 시점의 관계를 기준으로 HDD 단위 성능을 산출하는 평가 방식 |
    | 온라인 추론 (Online Inference) | 각 HDD의 관측값을 시간 순으로 입력하여 각 시점에서 고장 예측을 수행하는 과정 |
    | 최초 Alarm (First Alarm) | 한 HDD에서 의사결정 임곗값을 최초로 초과하여 발생한 Alarm |
    | Lead Time | 최초 Alarm 발생 시점부터 실제 고장 발생 시점까지의 시간 |
    | 예측 기간 (Prediction Horizon) | 최초 Alarm이 On-time으로 인정되는 고장 이전의 시간 범위 |
    | 우측 중도절단 (Right-censoring) | 관측 종료 시점까지 고장이 발생하지 않아 실제 고장 시점을 관측할 수 없는 상태 |
    | On-time Alarm | 예측 기간 이내에 발생한 최초 Alarm |
    | Early Alarm | 예측 기간을 초과하여 이른 시점에 발생한 최초 Alarm |
    | Censored Early Alarm | 실제 고장 시점이 관측되지 않은 우측 중도절단 HDD에서 평가 구간 내 발생한 최초 Alarm |
    | Missed Failure | 고장이 관측된 HDD에서 최초 Alarm이 발생하지 않은 경우 |
    | Censored No Alarm | 우측 중도절단 HDD에서 평가 구간 내 최초 Alarm이 발생하지 않은 경우 |
    | HDD-level Precision | 최초 Alarm이 발생한 전체 HDD 중 해당 Alarm이 On-time으로 분류된 HDD의 비율 |
    | HDD-level Recall | 실제 고장 HDD 중 On-time Alarm으로 탐지된 HDD의 비율 |
    | HDD-level FAR | 우측 중도절단 HDD 중 평가 구간 내 최초 Alarm이 발생한 HDD의 비율 |
    | Median Lead Time | 최초 Alarm이 발생한 고장 관측 HDD의 Lead Time 중앙값 |
    
    핵심은 행 단위 평가와 운영 환경 기반 평가의 차이가 단순한 평가 대상의 변경이나 HDD별 재집계에 있지 않다는 점이다. 행 단위 평가는 각 관측 행을 독립적으로 분류하지만, 운영 환경 기반 평가는 시간 순 온라인 추론, Alarm 변환, 최초 Alarm 판정 및 우측 중도절단 처리를 수행한다. 즉 HDD 단위의 시간적 Alarm 발생 과정을 평가 절차에 포함한다. 따라서 HDD-level Precision, Recall, FAR은 행 결과를 단순히 묶어 얻은 값이 아니라 별도의 평가 절차에서 산출된 결과이다.
    
    두 평가가 단순한 재집계가 아닌 근거는 다음과 같다. 행 단위 혼동행렬만으로는 최초 Alarm을 계산할 수 없고, 행 단위 레이블은 개별 행의 속성인 반면 운영 판정은 예측 궤적과 고장 시점의 관계에 기반한다. 또한 동일한 예측 결과와 임곗값을 사용했음에도 평가 방식에 따라 18개 모델 쌍 중 9개의 우열 방향이 바뀌었으며, Toshiba의 LightGBM은 30개 seed 모두에서 순위가 역전되었다. 따라서 본 연구에서는 이를 ‘평가 단위 변경’이 아니라 ‘평가 절차의 차이’로 표현한다.
    
    **연구 질문**
    
    - RQ1. 동일한 모델의 성능을 행 단위 평가와 운영 환경 기반 평가로 측정할 경우, 성능 평가 결과에 어떤 차이가 나타나는가?
    - RQ2. 운영 환경을 기준으로 임곗값을 최적화하면 기존 행 단위 최적화 결과와 어떤 차이가 나타나는가?
    - RQ3. 운영 환경 기반 성능평가는 기존 평가에서 확인하기 어려운 어떤 운영 정보를 제공하는가?

선행연구 정리

- 참고문헌
    
    
    | 문장 | 플레이스홀더 | 문헌 |
    | --- | --- | --- |
    | 고장 예측 연구가 지속적으로 수행되어 왔다 | `[HDD 신뢰성·예지보전 연구]` | **Pinheiro 2007** |
    | SMART … 주요 정보원으로 활용되어 왔다 | `[HDD 통계·SMART]` `[HDD 머신러닝 예측]` | **Botezatu 2016**, **Lu 2020** |
    | 교체의 대상은 개별 행이 아니라 HDD 자체 | `[HDD 통계·SMART]` `[HDD 교체·고장 예측]` | **Pinheiro 2007**, **Botezatu 2016** |
    | 개별 Row·Window 중심으로 Precision·Recall 산출 | `[행·Window 단위 평가]` | **LSTM individual specificity 2020**, **TCN-LSTM-Attention 2026** |
    | 최초 Alarm 시점·대응 시간·불필요 Alarm이 중요 | `[예지보전 평가 관련]` `[조기 시계열 분류]` | **Survey of PdM 2019**, **Gupta 2020** |
    
    | 문장 | 플레이스홀더 | 문헌 |
    | --- | --- | --- |
    | 통계적·신뢰성 분석에서 시작 | `[HDD 통계·SMART]` | **Pinheiro 2007** |
    | RF, SVM, XGBoost, LightGBM 적용 | `[HDD 교체·고장 예측]` | **Botezatu 2016**, **LightGBM with CID 2021** |
    | Sliding Window·LSTM·Transformer로 확장 | `[HDD 시계열·딥러닝]` `[RUL 예측]` | **LSTM individual 2020**, **TCN-LSTM-Attention 2026** / **TFBEST 2023** |
    | 평가 대상은 대체로 Row·Window | `[행·Window 단위 평가]` | **Model update strategies 2021**, **Cost-aware survival deepnet 2024** |
    | 클래스 불균형 / Concept Drift·온라인 / 설명 가능 | `[클래스 불균형]` `[Concept Drift·온라인]` `[설명 가능한 예측]` | **Imbalanced disk failures 2022** / **StreamDFP 2023** / **Explainable TS features 2025** |
    | 온라인 추론 적용이 운영 성능평가는 아니다 | `[Concept Drift·온라인]` | **StreamDFP 2023** |
    
    | 문장 | 플레이스홀더 | 문헌 |
    | --- | --- | --- |
    | 평가 대상·Horizon·레이블 정의가 연구마다 다름 | `[평가 절차 비교]` | **DFPoLD 2025**, **Explainable TS features 2025** |
    | 운영 관련 지표를 사용하고 전체 생애를 시간 순 처리 | `[운영 지표 활용]` | **StreamDFP 2023**, **LightGBM with CID 2021** |
    | 반복 Positive가 여러 결과로 분산 | `[행 단위 평가 관련]` → `[행·Window 단위 평가]`로 통합 | **Model update strategies 2021**, **Cost-aware survival deepnet 2024** |
    | **HDD 운영 단위에서도 우수하다고 단정하기 어렵다** | **`[운영 평가 관련]` 복구** | **Patient vs Prediction-Level 2026** |
    | 환자 단위 AUC 0.86 대 시점 단위 0.62 | `[Patient vs Prediction-Level]` | **Patient vs Prediction-Level 2026** |
    | 중복 예측·Alarm을 사건·개체 단위와 연결해 평가 | `[예지보전 평가]` `[연속 예측 평가]` `[조기 시계열 분류]` | **Survey of PdM 2019** / **Continuous Evaluation Frameworks 2026** / **Gupta 2020** |
    | 통합한 연구는 제한적이다 | `[평가 절차 비교]` | **StreamDFP 2023**, **Explainable TS features 2025** |
    
    Botezatu 2016 · RODMAN 2019 · Lu 2020 · LSTM individual 2020 · LightGBM with CID 2021 · Model update strategies 2021 · StreamDFP 2023 · Cost-aware survival deepnet 2024 · DFPoLD 2025 · Explainable TS features 2025 · TCN-LSTM-Attention 2026
    

하드 디스크 드라이브 모델 선정 기준

HDD 모델 선택

# SMART 기반 HDD 고장 예측 모델의 운영 환경 기반 성능 평가

## 요약

스토리지 운영에서 점검과 교체의 대상은 개별 관측 행이 아니라 HDD이지만, HDD 고장 예측 모델은 주로 각 관측 행을 독립적으로 분류하는 행 단위 평가로 측정되어 왔다. 이 방식은 몇 대의 HDD가 점검 대상이 되는지와 Alarm이 대응 가능한 시점에 발생했는지를 알려주지 못한다. 본 논문에서는 각 HDD의 관측 이력을 시간 순으로 처리하여 최초 Alarm과 실제 고장 또는 관측 종료 시점의 관계로부터 HDD 단위 지표와 Lead Time을 산출하는 운영 환경 기반 성능평가 방법을 제안한다. Backblaze 데이터 3종과 4개 모델, 30개 seed에 적용한 결과 행 단위 FAR 1%로 임곗값을 정하더라도 실제 점검 대상이 되는 HDD는 관측 기간 내 고장이 확인되지 않은 HDD의 2.33~8.77%에 이르렀으며, 이 값은 행 단위 지표로부터 환산할 수 없다. 또한 두 평가가 선택한 1위 모델은 60~87%의 seed에서 서로 달랐다. 제안 방법은 Alarm 시점과 HDD별 탐지 안정성 등 행 단위 평가가 산출할 수 없는 운영 정보를 제공한다.

Keywords: HDD failure prediction, SMART, operational evaluation, first alarm, right-censoring, predictive maintenance

## 1. 서론

데이터 저장 수요의 증가와 함께 하드 디스크 드라이브(Hard Disk Drive, HDD)는 대규모 저장 시스템과 데이터센터에서 널리 활용되고 있다. HDD 고장은 데이터 손실과 서비스 가용성 저하로 이어질 수 있으므로, 고장을 사전에 탐지하고 유지보수 의사결정에 활용하기 위한 고장 예측 연구가 지속적으로 수행되어 왔다 [HDD 신뢰성·예지보전 연구]. 특히 SMART(Self-Monitoring, Analysis and Reporting Technology)는 HDD의 상태 및 오류 관련 속성을 모니터링하고 기록하는 기술이며, SMART 데이터는 HDD의 상태를 시점별로 관측할 수 있는 주요 정보원으로 활용되어 왔다 [HDD 통계·SMART 연구].

HDD의 SMART 데이터는 HDD별로 시간에 따라 반복적으로 수집되며, 실제 운영에서 유지보수와 교체의 대상이 되는 것은 개별 관측 행이 아니라 HDD 자체이다. 따라서 동일 HDD에서 여러 시점에 발생한 예측 결과는 서로 완전히 독립적인 사건이라기보다 하나의 HDD에 대한 시간적 Alarm 과정으로 해석될 수 있다. 그럼에도 기존 HDD 고장 예측 연구에서는 개별 행 또는 특정 시점의 윈도우의 예측 결과를 중심으로 Precision, Recall, F1-score, AUC 및 FAR 등을 산출하는 경우가 많다 [행·Window 단위 평가 관련]. 이 경우 하나의 HDD에서 반복적으로 발생한 Positive 예측이 여러 개의 개별 결과로 집계되며, 동일 HDD에 대한 최초 Alarm과 후속 Alarm의 관계는 평가 결과에 직접 반영되지 않는다.

실제 운영에서는 고장을 한 번이라도 탐지했는지뿐만 아니라, 최초 Alarm이 언제 발생했는지, 고장 발생 전 충분한 대응 시간이 확보되었는지, Alarm이 반복적으로 발생했는지, 그리고 고장이 관측되지 않은 HDD에서 불필요한 Alarm이 발생했는지가 중요하다 [예지보전 평가 관련]. 특히 고장 전 너무 이른 시점에 발생한 Alarm은 실제 정비 의사결정에 유효한 Alarm으로 해석되지 않을 수 있으며, 관측 종료 전에 고장이 확인되지 않은 HDD는 이후 고장 가능성을 확인할 수 없으므로 별도의 처리가 필요하다. 따라서 HDD 고장 예측의 성능을 평가하기 위해서는 개별 관측 행의 분류 결과뿐만 아니라, 각 HDD의 관측 이력을 시간 순으로 처리하고 Alarm 발생과 실제 고장 또는 관측 종료 시점의 관계를 반영하는 평가 관점이 필요하다.

본 연구의 목적은 HDD의 시간적 관측 과정을 반영한 운영 환경 기반 성능평가 방법을 제안하고, 기존 행 단위 평가와 비교하여 평가 절차에 따른 성능 차이를 분석하는 데 있다. 여기서 '운영 환경 기반 평가'는 ① 관측 이력의 시간 순 온라인 추론, ② 예측확률의 Alarm 변환, ③ HDD 단위 최초 Alarm 판정, ④ 우측 중도절단 구간의 처리를 평가 절차에 포함하는 것을 의미한다. 반면 Alarm 억제, 최소 지속시간 및 재발생 대기, Alarm 단계화, 교체·점검 비용과 인력 운용 등 실제 운영의 대응 정책은 포함하지 않는다. 따라서 본 연구의 평가 대상은 운영 정책 전반이 아니라, 모델의 예측이 HDD 단위 Alarm으로 전환되기까지의 과정이다.

본 연구에서는 동일한 예측 결과에 행 단위 평가와 운영 환경 기반 평가를 각각 적용하고, 두 평가가 각자의 기준으로 임곗값을 최적화했을 때 성능지표와 모델 순위가 어떻게 달라지는지, 그리고 행 단위 평가로는 확인하기 어려운 어떤 운영 특성이 드러나는지를 분석한다.

---

## 2. 관련 연구

### 2.1 HDD SMART 기반 고장 예측 연구

HDD 고장 예측 연구는 고장 기록과 SMART 속성의 관계를 분석하는 통계적·신뢰성 분석에서 시작되었으며 [HDD 통계·SMART 연구], 이후 SMART 데이터로 고장이나 교체 여부를 직접 예측하는 머신러닝 이진 분류로 확장되어 Random Forest, SVM, XGBoost, LightGBM 등이 적용되었다 [HDD 교체·고장 예측 연구]. 이어 Sliding Window, LSTM, CNN-LSTM, Transformer와 같이 시간적 특성을 반영하는 모델이 도입되어 고장 여부 또는 잔여수명을 예측하였다 [HDD 시계열·딥러닝 연구] [RUL 예측 연구]. 다만 평가 대상은 대체로 개별 행 또는 윈도우이며, HDD 전체 생애에 걸친 연속적 추론과 최초 Alarm 기준의 운영 평가는 별도로 수행되지 않았다 [행·Window 단위 평가 관련].

최근에는 클래스 불균형 [클래스 불균형 연구], Concept Drift와 온라인 학습 [Concept Drift·온라인 학습 연구], 설명 가능한 예측 [설명 가능한 예측 연구]으로 범위가 넓어지고 있으나, 온라인 추론의 적용 자체가 최초 Alarm과 우측 중도절단을 포함한 운영 성능평가를 뜻하지는 않는다. 본 연구는 모델 구조의 개선보다 동일한 예측 결과를 운영 과정에 적용했을 때 성능이 어떻게 해석되는지에 초점을 둔다.

### 2.2 HDD 고장 예측의 평가와 운영 관점 연구

HDD 고장 예측 연구에서는 평가 대상, 고장을 얼마나 앞서 예측할 것인지를 정하는 Prediction Horizon의 길이, 양성 레이블의 정의 및 평가 종료 조건이 연구마다 다르게 설정되어 왔다 [평가 절차 비교 연구].

**표 1. 선행 연구의 평가 프로토콜 비교**

| Study | Pred. / Eval. unit | Eval. scope | Horizon | Oper. metrics |
| --- | --- | --- | --- | --- |
| Disk replacement (2016) | Row / Row | Partial window | 10–15 d | — |
| RODMAN (2019) | Row / HDD | Full (time-split) | Not fixed | FPR |
| SMARTer (2020) | Sequence / HDD | Full | 10 d | — |
| LSTM specificity (2020) | Window / Window | Not specified | 15 d | FAR |
| LightGBM + CID (2021) | Row·Window / HDD | Partial window | 10 d | DPF |
| Model update (2021) | Row / Row | Full | 10 d | FAR |
| StreamDFP (2023) | Row / HDD (daily) | Full | 30 d | Days in Advance |
| Survival deepnet (2024) | Row / Row | Full | 15 d | FAR |
| DFPoLD (2025) | Row / Row | Partial window | Varies | DPF |
| Explainable TS (2025) | Segment / HDD | Window segments | 5 d | FAR |
| TCN-LSTM-Attn (2026) | Window / Window | Balanced windows | 7 d | FAR |
| This work | Row / Row·HDD | Full | 30 d | FAR, Median LT |

예측 단위는 하나의 예측값이 산출되는 단위, 평가 단위는 성능지표가 집계되는 단위이다. 표 1의 11편 중 6편은 Row 또는 Window를 평가 단위로 사용한다. 평가 범위의 Full은 각 HDD의 관측 이력을 모두 사용함을 뜻한다. DFPoLD는 결측률에 따라 양성 레이블링 구간을 정하고, StreamDFP는 30일 이내 고장을 예측하면서 고장 전 20일을 레이블링에 사용한다. 따라서 본 연구의 Prediction Horizon 30일을 선행 연구의 30일과 같은 개념으로 볼 수 없다. 평가 범위와 클래스 구성도 마찬가지여서, 보고된 지표값을 연구 사이에서 직접 비교하기는 어렵다.

일부 연구는 FAR, Lead Time, Days in Advance, DPF와 같은 운영 관련 지표를 사용하고 HDD의 전체 생애 데이터를 시간 순으로 처리하기도 한다 [운영 지표 활용 연구]. 그러나 운영 관련 지표를 쓴다는 것과 HDD 운영 단위에서 평가한다는 것은 다르다. DPF(Days Prior to Failure)와 Days in Advance는 고장 시점과 최초 예측 시점의 간격이라는 점에서 본 연구의 Lead Time과 형태가 같으나, 판정을 가르는 기준이 아니라 탐지에 성공한 HDD의 평균값으로 보고된다. 한편 [n]은 같은 이름을 입력 구간 종료와 고장 시점 사이의 고정 버퍼를 가리키는 데 사용하여 의미가 또 다르다.

행 단위로 집계하면 한 HDD의 반복 Positive가 여러 결과로 분산되므로, 행 단위에서 우수한 모델이 HDD 운영 단위에서도 우수하다고 단정하기 어렵다 [행 단위 평가 관련] [운영 평가 관련]. HDD를 평가 단위로 삼은 연구도 고장 예측을 특정 시점의 상태를 판별하는 분류 문제로 정의한다. 그 결과 양성 레이블이 고장 시점을 기준으로 정의되어 각 HDD에 대한 판정이 고정 길이의 구간 단위로 이루어지고, 분류 결과가 범주이므로 Alarm의 발생 시점은 판정에 남지 않으며, 모든 HDD가 두 범주 중 하나에 속해야 하므로 고장이 확인되지 않은 HDD는 정상으로 처리된다. 시간창의 길이나 오탐 제약 수준은 데이터 이전에 필요한 시간이나 점검 부담과 같은 운영상의 이유로 정해지지만, 실제로 확보된 대응 시간과 점검 대상이 되는 HDD 대수는 측정 대상에 포함되지 않는다.

예지보전과 시계열 기반 의료 AI 등 연속 예측 분야에서도 개별 예측의 정확성과 개체 단위 성능이 서로 다를 수 있음이 보고되고 있다. 패혈증 예측에서는 동일한 모델이 환자 단위 평가와 시점 단위 평가에서 AUC 0.86과 0.62로 갈렸다 [Patient vs Prediction-Level]. 중복 예측과 Alarm을 실제 사건·개체 단위 결과와 연결하여 평가하는 방법이 제안되었다 [예지보전 평가 관련] [연속 예측 평가 관련] [조기 시계열 분류 관련]. 이들은 HDD 고장 예측에 직접 적용되는 평가 방법은 아니지만, 개체 단위 판정과 Alarm 시점을 평가에 포함하는 관점이 다른 분야에서는 이미 자리 잡았음을 보여준다. 저자가 확인한 범위에서, HDD의 전체 관측 이력에 시간 순 온라인 추론을 적용하고 최초 Alarm의 시점과 우측 중도절단을 함께 판정에 포함한 연구는 확인되지 않았다.

---

## 3. 평가 방법

### 3.1 행 단위 평가

행 단위 평가는 각 HDD의 개별 관측 행을 독립적인 분류 대상으로 취급하는 평가 방식으로, 표 1에서 복수의 사례로 나타나는 구성이다. 본 연구는 HDD 전체 관측 이력에 이를 적용한 결과를 비교 기준선으로 삼는다. HDD $d$의 관측 시점 $t$에 대한 예측확률 $p_{d,t}$가 의사결정 임곗값 $\tau$ 이상인 경우 해당 행을 양성으로 예측한 것으로 보고, 각 행의 예측 결과와 레이블을 대조하여 True Positive, False Positive, False Negative 및 True Negative를 집계한다. Precision, Recall, FAR은 이로부터 통상적인 방식으로 산출한다. 이때 각 행은 서로 독립적인 분류 결과로 취급되므로, 동일한 HDD에서 여러 시점에 발생한 양성 예측은 서로 구분되지 않고 개별 결과로 합산된다.

행 단위 레이블은 다음과 같이 정의한다. 고장 시점이 관측된 HDD에서 실제 고장까지 남은 기간이 Prediction Horizon 이내인 행, 즉 $t_{\mathrm{failure},d}-t \le H$인 행을 양성으로 정의하고 그 외의 행을 음성으로 정의한다. 우측 중도절단 HDD는 관측 종료 시점 $t_{\mathrm{end},d}$ 이후를 확인할 수 없어 종료 직전 $H$일 구간의 행은 레이블을 확정할 수 없으므로, 이 구간을 평가에서 제외한다. 평가 대상은 테스트 분할에 포함된 HDD의 전체 관측 이력 중 위 기준으로 제외되지 않은 모든 행이다. 

### 3.2 운영 환경 기반 평가

운영 환경 기반 평가는 HDD별 시간 순서에 따라 온라인 추론을 수행하고, 각 시점의 예측확률을 임곗값에 따라 Alarm으로 변환한 뒤 그 결과를 HDD 단위로 집계한다. 여기서 온라인 추론은 학습된 모델을 시간 순으로 적용하는 것을 뜻하며 모델 갱신을 포함하지 않는다. HDD를 $d$로 표기하고, 각 HDD의 SMART 관측값을 시간 순으로 정렬하여 운영 이력으로 구성한다. 평가 대상 HDD는 관측 기간 내 고장 시점이 확인된 HDD와 관측 종료 시점까지 고장이 확인되지 않은 우측 중도절단 HDD로 구분한다. 여기서 우측 중도절단은 HDD가 정상임을 의미하는 것이 아니라, 관측 종료 이후의 고장 여부와 고장 시점을 확인할 수 없음을 의미한다.

각 모델은 시간 순서에 따라 관측 시점별 고장 예측확률 $p_{d,t}$를 산출하며, $p_{d,t}\ge\tau$인 시점에 Alarm이 발생한다. 한 HDD에서 Alarm은 여러 번 발생할 수 있으므로 평가 대상 구간의 최초 Alarm 시점 $t_{\mathrm{alarm},d}$만을 판정에 사용한다. 고장 관측 HDD의 고장 시점을 $t_{\mathrm{failure},d}$라 할 때 Lead Time은 $\mathrm{LT}_d=t_{\mathrm{failure},d}-t_{\mathrm{alarm},d}$이며, 관측이 고장 시점까지 이루어지므로 음수가 될 수 없다. Prediction Horizon은 $H=30$일로 두고, $\mathrm{LT}_d\le H$이면 On-time, $\mathrm{LT}_d>H$이면 Early, 최초 Alarm이 없으면 Missed로 분류한다.

우측 중도절단 HDD는 관측 종료 시점 $t_{\mathrm{end},d}$ 이후의 고장 여부를 확인할 수 없으므로, 관측 종료 직전 $H$일 동안에는 해당 Alarm이 On-time인지 여부를 판단할 수 없다. 따라서 본 연구에서는 관측 종료 시점으로부터 $H$일에 해당하는 마지막 구간을 평가에서 제외하고, 그 이전에 발생한 최초 Alarm만을 평가 대상으로 한다. 평가 대상 구간에서 최초 Alarm이 발생한 우측 중도절단 HDD의 경우 실제 고장 시점은 관측 종료 이후에 존재할 가능성이 있으므로 정확한 Lead Time은 산출할 수 없다. 그러나 해당 Alarm이 관측 종료 시점보다 최소 $H$일 이전에 발생하였으므로, 향후 고장이 발생한다면 그 Lead Time은 $H$를 초과하게 된다. 이를 다음과 같이 나타낼 수 있다.

$$
\begin{aligned}
\mathrm{LT}_d
&=
t_{\mathrm{failure},d}-t_{\mathrm{alarm},d} \\
&>
t_{\mathrm{end},d}-t_{\mathrm{alarm},d}
\geq H
\end{aligned}
$$

즉, 실제 고장 시점이 관측되지 않았더라도 해당 Alarm은 On-time으로 분류될 수 없음이 보장된다. 따라서 이를 Censored Early로 분류하며, 평가 대상 구간에서 최초 Alarm이 발생하지 않은 우측 중도절단 HDD는 Censored No Alarm으로 분류한다. 이러한 판정 절차를 통해 각 HDD는 On-time, Early, Missed, Censored Early, Censored No Alarm의 다섯 가지 판정 결과 중 하나로 분류된다.

!그림 1. 고장 관측 HDD와 우측 중도절단 HDD의 평가 구간 및 판정 결과

그림 1. 고장 관측 HDD와 우측 중도절단 HDD의 평가 구간 및 판정 결과

그림 1에서 세로 파선은 관측 종료 시점이며, 우측 중도절단 HDD의 직전 $H$일 구간은 향후 고장 시점에 따라 On-time 여부가 달라져 판정할 수 없으므로 평가에서 제외한다.

이상의 판정 결과로부터 HDD-level Precision, Recall, FAR 및 Median Lead Time을 산출한다. 각 지표는 Alarm 발생 여부와 그 시간적 위치를 HDD 단위에서 평가하며, 네 지표의 모집단을 합치면 다섯 판정 결과가 모두 포함되어 각 HDD는 적어도 하나의 지표에 집계된다. 앞서 정의한 다섯 판정 결과를 $\mathrm{OT}$(On-time), $\mathrm{E}$(Early), $\mathrm{M}$(Missed), $\mathrm{CE}$(Censored Early), $\mathrm{CN}$(Censored No Alarm)으로 줄여 쓰고, 이들 각각을 $X$로 나타내어 해당 HDD의 집합을 $D_X$, 그 수를 $N_X$로 표기한다.

HDD-level Precision은 최초 Alarm이 발생한 HDD 중 On-time으로 판정된 HDD의 비율로 정의한다. Early와 Censored Early는 운영상 유효한 사전 Alarm으로 인정하지 않으므로, 이 지표는 Alarm 발생 빈도가 아니라 발생한 Alarm이 대응 가능한 시점에 발생했는지를 평가한다.

$$
\mathrm{Precision}_{\mathrm{HDD}}
=
\frac{N_{\mathrm{OT}}}
{N_{\mathrm{OT}}
+
N_{\mathrm{E}}
+
N_{\mathrm{CE}}}
$$

HDD-level Recall은 고장 시점이 관측된 HDD 중 Prediction Horizon 이내에 최초 Alarm이 발생한 HDD의 비율로 정의한다. 이 지표는 고장 전에 Alarm이 발생했는지만이 아니라 운영상 활용 가능한 Prediction Horizon 이내에 탐지했는지를 평가한다.

$$
\mathrm{Recall}_{\mathrm{HDD}}
=
\frac{N_{\mathrm{OT}}}
{N_{\mathrm{OT}}
+
N_{\mathrm{E}}
+
N_{\mathrm{M}}}
$$

HDD-level FAR은 관측 종료 시점까지 고장이 확인되지 않은 우측 중도절단 HDD 중 최초 Alarm이 발생한 HDD의 비율로 정의한다. 우측 중도절단 HDD의 Alarm을 오탐으로 세는 것은 해당 HDD가 향후 고장하지 않는다고 가정하는 것이 아니라, 평가 구간의 정의에 따라 그 Alarm 이후 최소 $H$일 동안 고장이 관측되지 않았음이 보장되어 관측된 범위 내에서 On-time 조건을 충족하지 못함을 의미한다. 이 지표는 평가 구간 전체에서 최초 Alarm의 발생 여부를 집계하므로 관측 기간이 길수록 값이 커지며, 관측 기간이 다른 데이터셋 사이의 우열 판단에는 사용하지 않는다.

$$
\mathrm{FAR}_{\mathrm{HDD}}
=
\frac{N_{\mathrm{CE}}}
{N_{\mathrm{CE}}
+
N_{\mathrm{CN}}}
$$

Median Lead Time은 최초 Alarm이 발생한 고장 관측 HDD의 Lead Time 중앙값으로 정의한다. On-time과 Early는 모두 최초 Alarm이 발생한 고장 관측 HDD이므로 함께 포함하며, 실제 고장 시점을 확인할 수 없는 우측 중도절단 HDD는 제외한다. 지나치게 긴 Lead Time은 Early Alarm의 증가를 의미할 수 있으므로, 이 지표는 클수록 좋은 단조적 성능 지표가 아니라 Alarm이 실제 고장 시점으로부터 어느 정도의 시간적 여유를 두고 발생하는지를 나타내는 운영 특성 지표로 활용한다.

$$
\mathrm{Median\ LT}
=
\operatorname{median}
\left\{
\mathrm{LT}_d
\mid
d\in
D_{\mathrm{OT}}
\cup
D_{\mathrm{E}}
\right\}
$$

---

## 4. 실험 설계

### 4.1 데이터 및 실험 설정

본 연구는 Backblaze가 공개하는 데이터센터 HDD의 SMART 기록을 사용하였다 [Backblaze]. 제조사가 다른 HDD 모델 3종(HGST HUH721212ALN604, Seagate ST12000NM0007, Toshiba MG07ACA14TA)을 선정하였으며, 세 모델은 HDD 수·고장률·관측 기간이 서로 달라 데이터 특성이 다른 조건에서 평가 결과를 비교할 수 있다(표 2). 이하에서는 각 데이터셋을 제조사명으로 지칭한다.

**표 2. 실험에 사용한 데이터셋의 특성**

|  | HGST | Seagate | Toshiba |
| --- | --- | --- | --- |
| HDDs | 11,370 | 38,790 | 39,351 |
| Failed HDDs (%) | 1,608 (14.1%) | 2,236 (5.8%) | 2,076 (5.3%) |
| Rows | 26,863,127 | 37,331,086 | 75,086,961 |
| Positive label rate | 0.176% | 0.181% | 0.083% |
| Obs. days, Med. (IQR) | 2,512 (2,423–2,587) | 962 (772–1,141) | 1,933 (1,772–2,065) |

세 데이터셋에 동일한 전처리를 적용하였다. Normalization 컬럼과 메타데이터 컬럼, 결측치 비율 90% 초과 컬럼 및 중복 행을 제거하고, 관측일 사이의 공백은 3일 이하이면 직전 값으로 보간, 4일 이상이면 별도의 시계열 세그먼트로 분리하였다. 이후 결측치가 남은 행과 고장 기록 후 다시 정상으로 기록된 HDD를 제거하였다. 전처리로 제거된 HDD는 데이터셋별 0.1~0.6%였고, 사용된 특성은 18~26개이다.

세그먼트 분리는 불연속 구간이 하나의 입력 윈도우에 섞이지 않도록 하며, HGST 기준 813대(7.1%)에 적용되었다. 입력 윈도우는 각 세그먼트 안에서 구성하되, 평가에서는 세그먼트가 분리된 HDD도 하나의 HDD로 취급하고 Lead Time은 달력일 기준으로 산출한다. 입력 구간을 온전히 확보할 수 없는 관측 초반부는 첫 행을 반복하여 채웠으므로, 네 모델 모두 동일한 행에서 예측확률이 산출된다.

고장 예측을 이진 분류로 정의하고 실제 고장일 이전 $H=30$일의 관측 시점을 양성으로 두었다. 우측 중도절단 HDD는 관측 종료 이후를 확인할 수 없으므로, 3.2의 평가 구간 정의와 동일하게 $t_{\mathrm{end},d}-t<H$인 행을 학습·검증·테스트에서 모두 제외하였다. 표 2의 양성 레이블 비율은 이 제외를 반영하지 않은 값이다.

동일 HDD가 두 분할에 걸치지 않도록, 고장 관측 여부로 층화한 HDD 단위 그룹 분할을 8:1:1로 적용하였다. 이는 특정 시점 이후를 예측하는 설정이 아니라 처음 보는 HDD에 대한 일반화 성능을 평가하기 때문이며, 각 테스트 HDD는 관측 이력 전체를 시간 순으로 하루씩 진행하며 추론하므로 HDD 내부의 시간 순서는 보존된다. 분할은 고정된 난수로 한 번만 수행하여 평가 대상 HDD 집합을 모든 실험에서 동일하게 두었고, 30개 random seed는 학습의 무작위성만 바꾼다. 다만 학습과 테스트가 같은 달력 구간을 공유하는 점은 두 평가에 동일하게 작용하므로, 절차 간 비교에는 영향을 주지 않는다.

2.1에서 정리한 두 계열을 대표하도록 트리 기반(LightGBM, XGBoost)과 시계열(LSTM, GRU) 모델을 선정하였다. 트리 기반 모델은 학습률 0.05에 최대 300 rounds로 학습하였고, 구조는 LightGBM이 Num Leaves 31 및 Min Data in Leaf 100, XGBoost가 Max Depth 6이며 표본·피처 추출 비율은 두 모델 모두 0.9이다. 시계열 모델은 은닉 64차원 2층에 FC 64–32–1을 연결하고 14일 입력 윈도우를 사용하였으며, Adam(학습률 0.001, Weight Decay 1e−5)과 Dropout 0.2로 최대 30 epochs, Batch 16,384에서 학습하였다. 시계열 모델에 한해 입력 피처를 학습 데이터에서만 적합한 StandardScaler로 표준화하고 [-10,10]으로 클리핑하였다.

학습 손실은 네 모델 모두 이진 교차 엔트로피이며, 검증 성능을 기준으로 Early Stopping을 적용하였다. 감시 지표는 트리 기반이 PR 곡선 기반 지표(LightGBM Average Precision, XGBoost AUC-PR)를 20 rounds, 시계열 모델이 검증 손실을 5 epochs로 두었다. 네 모델 모두 반복 상한 전에 조기 종료되어, 상한 설정의 차이는 모델 간 비교에 영향을 주지 않는다.

### 4.2 임곗값 선정 및 실험 구성

두 평가 방식은 각각의 기준으로 의사결정 임곗값을 선정한다. 검증 데이터에서 임곗값을 0.001 간격으로 탐색하되, 행 단위 평가에서는 행 단위 FAR이 1% 이하가 되는 최소 임곗값을 $\tau_{\mathrm{row}}$로, 운영 환경 기반 평가에서는 HDD-level FAR이 1% 이하가 되는 최소 임곗값을 $\tau_{\mathrm{op}}$로 선정하였다. 두 임곗값은 검증 데이터에서 독립적으로 선정한 뒤 동일한 테스트 데이터에 적용하였다. 검증 데이터에서 제약을 만족하는 임곗값이 탐색 범위 내에 존재하지 않는 경우에는 제약 위반이 가장 작은 임곗값을 사용하고 해당 seed도 집계에 포함하였다.

대규모 스토리지 시스템에서는 개별 HDD의 불필요한 Alarm도 시스템 규모에서는 상당한 운영 부담으로 누적되므로, 두 평가 방식 모두 오탐 수준을 제한하는 제약을 부여하였다. 이때 오탐 수준을 동일한 수치적 기준에서 비교하기 위해 행 단위 FAR과 HDD-level FAR에 모두 1%의 제약을 적용하였다. 다만 두 지표는 적용 모집단과 산출 방식이 다르므로, 동일한 1%가 동일한 운영적 엄격도를 의미하는 것은 아니다. 이러한 차이는 5.2절에서 분석한다.

평가 대상 행 집합은 두 평가에서 동일하게 두었으며, 예측확률 또한 하나의 온라인 추론 결과를 공유한다. 운영 환경 기반 평가는 각 HDD의 전체 관측 이력을 시간 순으로 처리하는 것을 전제로 하므로, 행 단위 평가를 이와 다른 행 집합에서 산출할 경우 두 평가의 차이가 평가 절차의 차이에서 비롯된 것인지 평가 대상 데이터의 차이에서 비롯된 것인지 구분할 수 없다.

이 구성에서는 양성 레이블 비율이 0.083~0.181%로 낮아, 본 연구의 행 단위 Precision은 고장 전 일부 구간이나 균형 표본으로 평가한 선행 연구의 보고값보다 낮게 나타난다. 본 연구의 비교는 절대 수준이 아니라 동일한 행 집합에서 평가 절차만 달리했을 때의 상대적 차이에 근거하므로 이 차이는 해석에 영향을 주지 않는다.

이상의 설정에서 평가 방식과 임곗값 기준을 조합한 네 가지 분석을 30개 random seed(42–71)에 대해 반복하였다. 이하에서 하나의 실험은 하나의 (데이터셋, 모델, seed) 조합이며, 모든 결과는 테스트 split에서 산출하였다. 조합별 30개 seed 결과는 중앙값으로 요약하고, 두 조건의 비교는 항상 동일한 실험 내에서 짝지어 수행한다. 30개 seed는 동일한 테스트 HDD 집합을 공유하는 재무작위화이므로 통계적 검정 대신 360개(30 seed × 12 조합) 비교의 방향 일관성을 제시한다. 다만 그림 3과 그림 4, 그리고 이들을 설명하는 5.4절의 수치는 HGST의 seed 42를 사례로 사용하므로 30개 seed 중앙값과 다르다.

---

## 5. 실험 결과

### 5.1 행 단위 평가와 운영 환경 기반 평가의 성능 비교

본 절에서는 동일한 모델 예측 결과와 동일한 임곗값에 두 평가를 각각 적용한 결과를 비교한다. 임곗값은 검증 데이터에서 행 단위 FAR 1% 이하를 만족하는 최소 임곗값 $\tau_{\mathrm{row}}$를 사용하였다. 표 3의 마지막 열은 운영 환경 기반 평가를 기준으로 재선정한 $\tau_{\mathrm{op}}$의 결과이며, 임곗값 기준에 따른 차이는 5.2절에서 분석한다.

**표 3. 평가 방식과 임곗값 기준에 따른 성능 비교**

| Dataset | Model | Row-level$(\tau_{\mathrm{row}})$ | HDD-level$(\tau_{\mathrm{row}})$ | HDD-level$(\tau_{\mathrm{op}})$ |
| --- | --- | --- | --- | --- |
|  |  | Prec / Rec / FAR | Prec / Rec / FAR / Med. LT | Prec / Rec / FAR / Med. LT |
| HGST | LightGBM | 0.075 / 0.440 / 0.96% | 0.089 / 0.105 / 7.47% / 221 | 0.302 / 0.086 / 1.13% / 56 |
|  | XGBoost | 0.081 / 0.469 / 0.95% | 0.071 / 0.086 / 7.93% / 229 | 0.263 / 0.068 / 1.13% / 64 |
|  | LSTM | 0.070 / 0.453 / 1.09% | 0.066 / 0.068 / 6.14% / 223 | 0.266 / 0.117 / 1.54% / 80 |
|  | GRU | 0.071 / 0.459 / 1.07% | 0.057 / 0.062 / 6.96% / 223 | 0.283 / 0.136 / 1.74% / 72 |
| Seagate | LightGBM | 0.067 / 0.370 / 0.97% | 0.252 / 0.302 / 3.53% / 32 | 0.439 / 0.236 / 1.14% / 11 |
|  | XGBoost | 0.065 / 0.391 / 1.05% | 0.209 / 0.300 / 4.76% / 38 | 0.419 / 0.249 / 1.15% / 14 |
|  | LSTM | 0.065 / 0.379 / 1.02% | 0.167 / 0.273 / 6.02% / 47 | 0.354 / 0.182 / 1.08% / 24 |
|  | GRU | 0.070 / 0.386 / 0.97% | 0.171 / 0.284 / 6.18% / 45 | 0.412 / 0.224 / 1.04% / 21 |
| Toshiba | LightGBM | 0.084 / 0.392 / 0.36% | 0.193 / 0.196 / 2.33% / 68 | 0.306 / 0.148 / 0.55% / 45 |
|  | XGBoost | 0.044 / 0.462 / 0.88% | 0.160 / 0.220 / 4.04% / 76 | 0.282 / 0.163 / 0.89% / 46 |
|  | LSTM | 0.039 / 0.478 / 1.03% | 0.082 / 0.177 / 8.25% / 112 | 0.275 / 0.134 / 0.79% / 52 |
|  | GRU | 0.038 / 0.478 / 1.03% | 0.077 / 0.177 / 8.77% / 113 | 0.287 / 0.148 / 0.86% / 48 |

임곗값을 $\tau_{\mathrm{row}}$로 고정한 채 평가 방식만 바꾸면 세 지표가 서로 다른 방향으로 이동한다. 즉 운영 환경 기반 평가의 결과는 행 단위 결과를 일정한 비율로 환산하거나 재집계하여 얻을 수 없다. FAR은 행 단위에서 0.36~1.09%로 유지되는 동안 HDD-level에서는 2.33~8.77%로 나타났고 Recall은 HDD-level 값이 더 낮았으며(HGST의 LightGBM 0.440 → 0.105), 두 방향 모두 360개 짝지은 비교 전부에서 성립하였다. 반면 Precision은 방향이 일정하지 않아 HGST에서는 네 모델 중 셋에서 오히려 낮아졌다. HDD-level Precision이 Early의 비중에 좌우되기 때문이다. 행 단위 Precision이 0.065~0.070과 0.070~0.081로 비슷한 Seagate와 HGST를 비교하면, Median Lead Time이 32~47일인 Seagate에서는 HDD-level Precision이 0.167~0.252로 올라가는 반면 221~229일인 HGST에서는 최초 Alarm 대부분이 Early로 분류되어 0.057~0.089에 머문다.

FAR과 Recall의 차이는 서로 다른 이유에서 발생한다. 행 단위 FAR은 개별 행의 오탐 비율이므로 동일 HDD에서 반복된 Alarm이 운영 부담으로 누적되는 정도를 나타내지 못하는 반면, HDD-level FAR은 실제로 몇 대가 점검 대상이 되는지를 직접 나타낸다. Recall의 두 값은 성능의 높낮이가 아니라 서로 다른 대상을 측정한 결과다. HGST의 LightGBM에서는 고장이 관측된 162대 중 Alarm이 발생한 HDD가 116대에 이르렀음에도 On-time은 17대에 그쳤다(Early 99대, Missed 46대).

두 평가의 값이 다르다는 사실보다 중요한 것은 어느 쪽이 운영 의사결정에 필요한 정보인가이다. 유지보수 계획은 개별 관측 행이 아니라 HDD를 대상으로 수립되므로, 운영자가 알아야 하는 것은 오탐 행의 비율이 아니라 실제로 몇 대가 점검 대상이 되고 그 Alarm이 대응 가능한 시점에 발생했는지이다. 행 단위 FAR 1%는 이 질문에 답하지 못하며, 우측 중도절단 HDD의 2.33~8.77%에서 최초 Alarm이 발생한다는 사실과 그 이후 확보되는 대응 시간은 운영 환경 기반 평가를 통해서만 확인된다.

### 5.2 운영 환경 기준 임곗값 최적화 결과

5.1절에서 동일한 $\tau_{\mathrm{row}}$로도 두 평가의 결과가 크게 달라짐을 확인하였다. 본 절에서는 운영 환경 기반 평가를 기준으로 임곗값을 다시 선정했을 때의 변화를 분석한다. 두 기준이 고른 임곗값 자체가 크게 달라, $\tau_{\mathrm{op}}$는 $\tau_{\mathrm{row}}$의 3.9~127.5배였다(조합별 중앙값). 행 단위 FAR 1% 제약으로 정한 임곗값이 HDD-level FAR을 같은 수준으로 제한하지 못한 것이다.

$\tau_{\mathrm{op}}$로 바꾸면 오탐 부담이 크게 줄고 발생한 Alarm의 적시성이 높아진다(12개 조합 중앙값 기준 HDD-level FAR 6.16%→1.10%, Precision 0.124→0.295, Median Lead Time 93.5일→46.8일). 이 세 방향은 12개 조합 전부와 360개 비교 전부에서 예외가 없었다. 다만 Median Lead Time은 단조적 성능 지표가 아니므로 이 감소는 성능 향상이 아니라, 임곗값 상승에 따른 개별 HDD의 Lead Time 변화와 최초 Alarm이 사라진 HDD의 모집단 제외가 함께 반영된 결과다.

반면 HDD-level Recall은 임곗값에 대해 단조적이지 않다. 중앙값은 0.187에서 0.148로 낮아졌으나 12개 조합 중 2개에서는 상승하였고, HGST의 LSTM(0.068→0.117)과 GRU(0.062→0.136)는 30개 seed 전부에서 상승하였다. 임곗값이 오르면 최초 Alarm이 뒤로 밀려 Early였던 HDD가 Prediction Horizon 안으로 들어오기 때문이다. 이는 두 평가가 같은 예측 결과를 다르게 판정하는 데서 오는 구조적 차이로, 운영 성능을 행 단위 결과로부터 유추할 수 없다는 5.1절의 결과를 뒷받침한다.

임곗값 선정 절차에는 몇 가지 주의할 점이 있다. 두 임곗값 모두 검증 데이터에서 고른 값이므로 테스트 FAR은 제약선을 넘을 수 있다. 행 단위에서는 12개 조합 중 6개가 1%를 소폭 상회하였으나(1.02~1.09%), 운영 환경 기준에서는 8개가 더 큰 폭으로 상회하였다(1.04~1.74%). HDD-level FAR이 소수의 사건으로 추정되기 때문으로, HGST에서 Alarm이 발생한 중도절단 HDD는 977대 중 11~17대에 불과하다. 또한 HGST의 LightGBM은 예측확률이 1.0에 포화되는 HDD가 있어 탐색 상한(0.999)으로도 Alarm을 억제하지 못해 30개 seed 중 23개만 제약을 만족하였고, Toshiba의 LightGBM은 $\tau_{\mathrm{row}}$가 탐색 범위 하단에 놓여 행 단위 FAR이 다른 모델보다 낮다.

두 기준은 같은 1%를 쓰지만 적용 모집단이 달라 실질 엄격도가 같지 않다. 행 단위 FAR 1% 임곗값에서 이미 상당 비율의 중도절단 HDD에 최초 Alarm이 발생하므로(5.1절) $\tau_{\mathrm{op}}$가 훨씬 보수적이다. 따라서 본 절의 결과는 엄격도를 통제한 비교가 아니라 각 평가가 자신의 기준에서 같은 수치 제약을 적용했을 때의 차이이며, 엄격도를 맞춘 비교는 향후 과제로 남긴다.

### 5.3 평가 조건에 따른 모델 선택의 변화

5.1과 5.2는 30개 seed의 중앙값으로 두 조건을 비교하였다. 본 절에서는 같은 결과가 모델 선택이라는 의사결정 수준에서 어떤 차이를 만드는지를 seed 단위로 확인한다.

**표 4. 평가 조건 변경에 따른 1위 모델의 변경 비율**

| Changed condition | HGST | Seagate | Toshiba |
| --- | --- | --- | --- |
| Seed only (control) | 50% | 55% | 53% |
| Evaluation method | 97% | 63% | 87% |
| Threshold criterion | 100% | 37% | 47% |
| Both | 73% | 60% | 87% |

두 평가 모두 FAR을 1% 이하로 제약하였으므로, 같은 오탐 수준에서 더 많은 고장을 탐지하는 모델, 즉 각 seed에서 Recall이 가장 높은 모델을 1위로 판정하였다. 표시한 조건만 바꾸었을 때 1위 모델이 달라진 비율이며, 값이 클수록 그 조건이 모델 선택을 크게 바꾼다. 동점인 경우 공동 1위 모델의 집합이 완전히 일치할 때만 유지로 집계하였다. 세 조건 행은 30개 seed 각각에서, Seed only 행은 평가 조건을 고정한 채 30개 seed의 모든 쌍(435쌍)에서 비교한 대조군이다.

평가 방식만 바꾼 경우(표 4의 Evaluation method) 1위 모델은 대부분의 seed에서 달라졌다. HGST에서는 30개 seed 중 29개(97%)에서, Toshiba에서도 87%에서 1위가 바뀌었다. 이동 방향 또한 일관되어, LightGBM은 행 단위 평가에서 세 데이터셋 모두 최하위였으나 운영 환경 기반 평가에서는 모두 상위권으로 올라섰고 GRU는 반대 방향으로 이동하였다. 모델 쌍 단위로 보면 18개 쌍 가운데 9개에서 우열의 다수 방향이 뒤바뀌었으며, 특히 Toshiba의 LightGBM은 LSTM 및 GRU에 대해 행 단위 평가에서 30개 seed 전부 열세였으나 운영 환경 기반 평가에서는 30개 seed 전부 우세로 나타났다. 두 평가에 동일한 예측값과 동일한 임곗값을 사용하였으므로, 이 변화는 두 평가 절차가 성능을 판정하는 방식의 차이에 귀속된다.

임곗값 기준만 바꾼 경우(Threshold criterion)의 영향은 데이터셋에 따라 갈렸다. HGST에서는 30개 seed 전부에서 1위가 바뀌었으며(100%), $\tau_{\mathrm{row}}$ 기준으로 26개 seed에서 1위였던 LightGBM이 $\tau_{\mathrm{op}}$에서는 GRU 22개, LSTM 5개, 두 모델 공동 1위 3개로 나타나 트리 기반에서 순환신경망 기반으로 1위 모델군이 전환되었다. 반면 Seagate와 Toshiba에서는 변경 비율이 37%, 47%에 그쳐 절반 이상의 seed에서 1위가 유지되었다.

두 변경을 함께 적용한 경우(Both)는 각 평가를 실제로 사용하는 방식에 해당한다. 이때 1위 모델의 변경 비율은 60~87%였다. HGST에서는 73%로 두 변경을 각각 적용했을 때(97%, 100%)보다 오히려 낮아, 두 변경이 모델 순위에 미치는 영향이 부분적으로 상쇄된 것으로 보인다.

이러한 변화가 학습의 무작위성만으로 설명되지 않는다는 점은 표 4의 Seed only 행과의 대조에서 확인된다. 평가 조건을 고정한 채 seed만 달리했을 때에도 1위 모델은 50~55%에서 달라져, 반복 학습만으로도 인접한 모델 간 순위는 흔들린다. 그럼에도 평가 방식을 바꾼 경우의 변경 비율은 63~97%로 이보다 높았으며, 특히 HGST에서는 seed 간 50%인 변경 비율이 평가 방식 변경 시 97%까지 올라갔다. 다만 Seagate와 Toshiba에서 임곗값 기준만 바꾼 경우의 변경 비율(37%, 47%)은 seed 간 변경 비율보다 낮아, 이 두 데이터셋에서는 임곗값 기준의 영향이 학습 변동을 넘어서지 않았다.

본 절의 결과는 비교 대상 모델이 4개로 제한된 조건에서 얻은 것이며, seed 역시 동일한 테스트 HDD 집합을 공유한다. 따라서 관찰된 역전의 빈도를 모집단 수준의 일반적 현상으로 확대 해석하기보다는, 동일한 예측 결과에 어떤 평가 절차와 임곗값 기준을 적용하는지가 모델 선택 결과를 바꿀 수 있음을 본 실험 조건에서 반복적으로 확인한 결과로 한정하여 해석한다.

### 5.4 운영 환경 기반 평가를 통한 HDD별 운영 특성 분석

운영 환경 기반 평가는 HDD별 Alarm의 발생 시점을 유지하므로, 집계 지표만으로는 확인할 수 없는 운영 특성을 함께 분석할 수 있다.

!그림 2. HDD별 On-time 탐지 결과 (HGST, 30개 seed, τ_op)

그림 2. HDD별 On-time 탐지 결과 (HGST, 30개 seed, τ_op)

그림 2에서 (a)는 30개 seed 중 한 번도 On-time으로 탐지되지 않은 HDD 수로, 회색은 어떤 seed에서도 Alarm이 발생하지 않은 HDD를, 모델별 색상은 Alarm이 발생하였으나 그 Alarm이 모두 Early로 판정된 HDD를 나타낸다. (b)는 한 번 이상 탐지된 나머지 HDD가 30개 seed 중 몇 개에서 On-time으로 판정되었는지의 분포로, 오른쪽으로 갈수록 seed가 바뀌어도 반복해서 적시 탐지되는 HDD이다.

최선의 단일 모델이 30개 seed 내내 놓친 HDD는 109대였으나 네 모델을 모두 합치면 87대(54%)로 줄어, 22대는 모델을 바꾸면 탐지된다. 반면 57대(35%)는 어떤 조합에서도 Alarm이 발생하지 않아, 놓치는 집단은 모델 선택에 좌우되는 부분과 그렇지 않은 부분으로 나뉜다.

적시 탐지 이력이 있는 HDD에서도 탐지는 안정적이지 않았다. 한 번 이상 On-time으로 탐지된 HDD의 탐지 횟수 중앙값은 LightGBM, XGBoost 및 LSTM에서 30회 중 5회에 그쳤고, 30개 seed 전부에서 On-time으로 탐지된 HDD는 트리 기반 두 모델에서 하나도 없었으며 LSTM에서도 3대였다. 반면 GRU는 중앙값이 12회였고 26회 이상 탐지된 HDD가 15대, 30개 seed 전부에서 탐지된 HDD가 6대로, 해당 실험 조건에서 반복적으로 적시 탐지되는 HDD 집합이 상대적으로 뚜렷하게 나타났다.

행 단위 평가 지표만으로는 동일한 HDD를 반복적으로 놓치는지, 아니면 매번 다른 HDD를 놓치는지를 구분하기 어렵다. 이 결과는 낮은 HDD-level Recall이 평균적인 성능 부족만으로 설명되지 않으며, 특정 운영점에서 반복적으로 Missed로 판정되거나 Alarm이 발생하더라도 Early로만 판정되는 HDD 집단이 존재함을 보여준다.

!그림 3. 최초 Alarm의 Lead Time 분포 (HGST, XGBoost·GRU, seed 42, τ_op)

그림 3. 최초 Alarm의 Lead Time 분포 (HGST, XGBoost·GRU, seed 42, τ_op)

그림 3에서 세로축은 HDD 수이고 적색 파선은 중앙값이다. 360일을 초과하는 표본은 축 우측 끝의 빗금 막대에 합산하였으며, 중앙값은 축 범위 밖 표본을 포함하여 산출하였다.

그림 3에서 XGBoost와 GRU의 Lead Time 중앙값은 각각 57일과 75일로 Prediction Horizon(30일)을 크게 넘으며, 나머지 두 모델도 LightGBM 105일, LSTM 77일로 같은 양상이다. 최초 Alarm이 발생한 고장 관측 HDD 중 On-time은 27~39%뿐이고, 나머지 61~73%는 고장 전에 Alarm이 발생했음에도 Horizon 밖이라 Early로 분류되었다. 즉 낮은 HDD-level Recall은 탐지 실패뿐 아니라 지나치게 이른 최초 Alarm에서도 발생하며, 행 단위 평가는 두 경우를 구분하지 못한다.

모델 간 차이는 그림 3에서 직접 드러난다. (b)의 GRU는 (a)의 XGBoost보다 넓은 분포에 걸쳐 더 많은 HDD에서 최초 Alarm이 발생하여 그 수가 63대와 36대로 갈리며, On-time 비율은 36.5%와 38.9%로 비슷하지만 On-time HDD 수는 23대와 14대가 된다. 나머지 두 모델도 LSTM 65대, LightGBM 37대로 같은 대비를 보여, 순환신경망 계열이 트리 기반의 약 1.8배의 HDD에서 최초 Alarm이 발생한다. 5.3에서 $\tau_{\mathrm{op}}$ 기준 1위가 순환신경망 계열로 전환된 것과 같은 방향의 관찰이다. 다만 LightGBM은 seed별 $\tau_{\mathrm{op}}$ 변동이 커 표 3의 Median Lead Time과 차이가 특히 크다.

최초 Alarm 이후 반복되는 Alarm의 부담도 확인된다. 고장 관측 HDD에서 발생한 전체 Alarm은 LightGBM 915건, XGBoost 1,505건, LSTM 4,607건, GRU 2,828건으로 최초 Alarm이 발생한 HDD 한 대당 약 25·42·71·45건이며, 모델에 따라 약 3배까지 차이가 난다. LSTM은 최초 Alarm의 중앙값이 77일인데 전체 Alarm의 중앙값은 117일로, Alarm이 고장에서 먼 구간에 오래 누적된다. False Alarm의 시점도 보존되어 네 모델 모두 관측 후반부에 몰렸으나(1,400일 이전 0~3건), FAR 1% 제약으로 표본이 977대 중 10대 안팎이라 경향 이상의 해석은 어렵다.

!그림 4. 운영 환경 기반 평가의 네 가지 판정 사례에 대한 운영 타임라인 (HGST, GRU, seed 42)

그림 4. 운영 환경 기반 평가의 네 가지 판정 사례에 대한 운영 타임라인 (HGST, GRU, seed 42)

그림 4는 네 판정 사례를 각 HDD 관측 이력의 마지막 1년 구간에 대해 예측확률 시계열로 나타낸 것이다(청색 실선 예측확률, 적색 파선 임곗값, 적색 점선 최초 Alarm, 흑색 파선 고장). On-time 사례(a)는 예측확률이 장기간 낮다가 고장 직전 급상승해 16일 전에 최초 Alarm이 발생하고 이후 고장까지 임곗값을 상회한다. 반면 Early 사례(b)는 고장 332일 전 최초 Alarm 이후 약 250일에 걸쳐 임곗값을 6회 다시 상회하면서도 정작 고장에 근접한 구간에서는 임곗값 아래로 내려가, 이른 최초 Alarm이 Alarm 부담만 남기고 적시 탐지로 이어지지 않을 수 있음을 보여준다. Censored Early 사례(c)는 단발성 상승 한 번이 임곗값을 넘은 뒤 예측확률이 곧바로 떨어져 남은 약 8개월 동안 다시 넘지 않았으나, 그 Alarm이 관측 종료보다 $H$일 이상 앞서 HDD-level FAR의 오탐으로 계상된다. Missed 사례(d)는 예측확률이 완만히 상승해 고장 전 임곗값에 근접하였으나 끝내 넘지 못한 경우로, Missed 판정이 신호의 부재가 아니라 임곗값과의 관계에서 결정됨을 보여준다.

이상의 분석은 운영 환경 기반 평가가 탐지 여부나 집계된 성능 수준을 넘어, Alarm이 어떤 HDD에서 언제 발생하고 고장까지 얼마나 앞서며 이후 어떻게 반복되는지를 확인할 수 있게 함을 보여준다. 이러한 정보는 Alarm 정책의 설계 근거로 활용될 수 있으며, 반복적으로 탐지되지 않는 HDD 집단의 특성은 특징 기여도 분석 [설명 가능한 예측 연구]과 결합하여 규명할 수 있다.

---

## 6. 결론

본 연구는 개별 관측 행의 분류 정확도만으로는 HDD 단위 Alarm 과정의 성능을 반영하기 어렵다는 문제의식에서 출발하였다. 이에 각 HDD의 SMART 이력을 시간 순으로 처리해 온라인 추론을 수행하고, 최초 Alarm과 고장 또는 관측 종료 시점의 관계로 각 HDD를 다섯 판정 결과 중 하나로 구분하는 운영 환경 기반 평가를 구성하였다. 3개 데이터셋과 4개 모델, 30개 random seed에 동일한 예측 결과를 두고 두 평가 절차와 두 임곗값 기준을 적용하였다.

같은 예측 결과와 같은 임곗값에서도 두 평가는 다른 성능을 나타냈다. 행 단위 FAR이 약 1%인 조건에서 HDD-level FAR은 2.33~8.77%였고, HGST의 LightGBM은 Recall이 행 단위 0.440에서 HDD-level 0.105로 떨어졌다. 임곗값을 HDD-level FAR 기준으로 재선정하면 오탐 부담이 줄고 Alarm의 적시성이 높아졌으며, 이 두 방향은 360개 실험 전부에서 예외가 없었다. 반면 Recall은 단조적이지 않아 중앙값은 감소하였으나 일부 조합에서는 상승하였다.

평가 조건은 모델 선택까지 바꾸었다. 평가 방식만 바꾸었을 때 18개 모델 쌍 중 9개에서 우열의 다수 방향이 뒤바뀌었고, 임곗값 기준만 바꾸었을 때 HGST에서는 30개 seed 전부에서 1위가 달라져 $\tau_{\mathrm{op}}$에서는 모두 순환신경망 계열이 1위였다. 각 평가를 자신의 기준으로 적용했을 때 1위가 달라진 비율은 60~87%에 이르렀다. 나아가 운영 환경 기반 평가는 HDD별 적시 탐지의 안정성, 최초 Alarm의 Lead Time, 반복 Alarm과 False Alarm의 시간적 위치라는 운영 정보를 함께 제공하였다.

본 연구의 의의는 평가 대상을 개별 행의 분류 정확성에서 HDD의 시간적 Alarm 성능으로 옮기고, 우측 중도절단을 포함한 다섯 판정 결과로 각 지표의 모집단을 명시한 데 있다. 따라서 운영에 쓸 모델과 임곗값을 정할 때에는 행 단위 결과를 그대로 옮기기보다, 언제 위험을 감지하고 얼마의 여유가 확보되며 오탐이 언제 발생하는지를 함께 제시하는 운영 관점의 평가를 병행해야 한다.

이 결과는 Backblaze 데이터셋 3종과 예측 모델 4개라는 제한된 조건에서 얻은 것이므로 다른 설비로 일반화하기 어렵고, 기준선으로 삼은 행 단위 평가도 Sliding Window 등 다른 절차와의 차이를 검증할 필요가 있다. 그룹 분할로 학습과 테스트가 같은 달력 구간을 공유한다는 점, FAR 1%와 Prediction Horizon 30일이 비교를 위한 설정이라는 점도 한계이며, 시간 분할과 다양한 $H$·FAR 제약에서의 검증이 뒤따라야 한다.

제안한 평가 방법 자체에도 남은 문제가 있다. 이 절차는 고장이 관측된 HDD와 충분히 긴 중도절단 이력을 함께 요구하므로 관측 규모나 기간이 작은 환경에는 적용하기 어렵고, HDD-level FAR이 소수 사건으로 추정되는 것(HGST에서 977대 중 11~17대)도 같은 제약에서 비롯되어 임곗값 선정의 안정성에까지 영향을 준다. 이 지표는 우측 중도절단이 고장 위험과 독립이라는 가정에도 의존한다. 생존 분석 [Survival deepnet]과 같이 중도절단을 확률 모형에 직접 반영하면 이 가정을 완화할 수 있으나, 그 경우 다섯 판정 결과가 갖는 직접적인 운영 해석은 일부 포기하게 된다. 또한 본 평가는 예측이 HDD 단위 Alarm으로 전환되기까지를 대상으로 하므로, Alarm 정책과 재학습을 포함한 동적 운영 환경으로의 확장이 향후 과제로 남는다.

반면 제안한 절차는 SMART나 HDD 고유의 특성에 의존하지 않는다. 개체가 반복 관측되고 고장이 시점을 갖는 사건이며 관측 종료 시 다수가 중도절단되는 예지보전 문제라면 동일하게 적용할 수 있으나, 본 연구는 HDD에 한정하여 검증하였으므로 도메인 독립적인 프레임워크로의 정식화는 향후 과제로 남는다.