정보기술학회 게재, KCI 등재 목표

- 데이터셋
    
    https://drive.google.com/drive/folders/18TOKVO7WfFPBD3UTUz4EEedY6UjSC2ct?usp=sharing
    

[[테스트] 롤링추론 결과 및 비교](https://app.notion.com/p/3a980c45869980a09384d4c27cbcd6ba?pvs=21)

kci 평가기준 

구버전

# 제목: SMART 기반 HDD 고장 예측 모델의 운영 환경 기반 성능 평가

> 
> 
> 
> #### 운영 환경을 반영한 성능평가 방법을 제안하고, 기존 평가와 비교하여 그 필요성을 보이는 것
> 
- 참고
    
    
    | 용어 | 뜻 |
    | --- | --- |
    | 행 단위 평가 (Row-level evaluation) | 각 관측 행을 독립적인 분류 대상으로 평가하는 방식 |
    | 운영 환경 기반 평가 (Operational evaluation) | HDD의 시간 순차적 관측 과정을 유지한 채, 최초 Alarm과 실제 고장 또는 관측 종료 시점의 관계를 기준으로 HDD 단위 성능을 산출하는 평가 방식 |
    | 온라인 추론 (Online Inference) | 각 HDD의 관측값을 시간 순으로 입력하여 각 시점에서 고장 예측을 수행하는 과정 |
    | 최초 Alarm (First Alarm) | 한 HDD에서 의사결정 임곗값을 최초로 초과하여 발생한 Alarm |
    | Lead Time | 최초 Alarm 발생 시점부터 실제 고장 발생 시점까지의 시간 |
    | 예측 기간 (Prediction Horizon) | 최초 Alarm이 On-time으로 인정되는 고장 이전의 시간 범위 |
    | 우측 중도절단 (Right-censoring) | 관측 종료 시점까지 고장이 발생하지 않아 실제 고장 시점을 관측할 수 없는 상태 |
    | On-time Alarm | 예측 기간 이내에 발생한 최초 Alarm |
    | Early Alarm | 예측 기간을 초과하여 이른 시점에 발생한 최초 Alarm |
    | Censored Early Alarm | 실제 고장 시점이 관측되지 않은 우측 중도절단 HDD에서 평가 구간 내 발생한 최초 Alarm |
    | Missed Failure | 고장이 관측된 HDD에서 최초 Alarm이 발생하지 않은 경우 |
    | Censored No Alarm | 우측 중도절단 HDD에서 평가 구간 내 최초 Alarm이 발생하지 않은 경우 |
    | HDD-level Precision | 최초 Alarm이 발생한 전체 HDD 중 해당 Alarm이 On-time으로 분류된 HDD의 비율 |
    | HDD-level Recall | 실제 고장 HDD 중 On-time Alarm으로 탐지된 HDD의 비율 |
    | HDD-level FAR | 우측 중도절단 HDD 중 평가 구간 내 최초 Alarm이 발생한 HDD의 비율 |
    | Median Lead Time | 최초 Alarm이 발생한 고장 관측 HDD의 Lead Time 중앙값 |
    
    핵심은 행 단위 평가와 운영 환경 기반 평가의 차이가 단순한 평가 대상의 변경이나 HDD별 재집계에 있지 않다는 점이다. 행 단위 평가는 각 관측 행을 독립적으로 분류하지만, 운영 환경 기반 평가는 시간 순 온라인 추론, Alarm 변환, 최초 Alarm 판정 및 우측 중도절단 처리를 수행한다. 즉 HDD 단위의 시간적 경보 과정을 평가 절차에 포함한다. 따라서 HDD-level Precision, Recall, FAR은 행 결과를 단순히 묶어 얻은 값이 아니라 별도의 평가 절차에서 산출된 결과이다.
    
    두 평가가 단순한 재집계가 아닌 근거는 다음과 같다. 행 단위 혼동행렬만으로는 최초 Alarm을 계산할 수 없고, 행 단위 레이블은 개별 행의 속성인 반면 운영 판정은 예측 궤적과 고장 시점의 관계에 기반한다. 또한 동일한 예측 결과와 임곗값을 사용했음에도 평가 방식에 따라 18개 모델 쌍 중 9개의 우열 방향이 바뀌었으며, TOSHIBA의 LightGBM은 30개 seed 모두에서 순위가 역전되었다. 따라서 본 연구에서는 이를 ‘평가 단위 변경’이 아니라 ‘평가 절차의 차이’로 표현한다.
    
    **연구 질문**
    
    - RQ1. 동일한 모델의 성능을 행 단위 평가와 운영 환경 기반 평가로 측정할 경우, 성능 평가 결과에 어떤 차이가 나타나는가?
    - RQ2. 운영 환경을 기준으로 임곗값을 최적화하면 기존 행 단위 최적화 결과와 어떤 차이가 나타나는가?
    - RQ3. 운영 환경 기반 성능평가는 기존 평가에서 확인하기 어려운 어떤 운영 정보를 제공하는가?

## 초록

본 연구는 HDD 고장 예측 모델의 성능을 **HDD의 시간적 운영 과정을 반영하여** 평가하기 위한 운영 환경 기반 성능평가 방법을 제안한다. 기존의 행 단위 평가는 각 관측 행의 예측 결과를 독립적인 분류 대상으로 취급하므로, 하나의 HDD에서 시간에 따라 반복적으로 발생하는 예측과 Alarm, 최초 Alarm 시점 및 실제 고장까지의 대응 시간을 충분히 반영하기 어렵다. 이를 보완하기 위해 본 연구에서는 Backblaze HDD 데이터를 대상으로 각 HDD의 SMART 관측 이력을 시간 순으로 처리하여 온라인 추론을 수행하고, 예측 확률을 Alarm으로 변환한 뒤 최초 Alarm과 실제 고장 또는 관측 종료 시점의 관계를 이용하여 On-time, Early, Censored Early, Missed 및 Censored No Alarm을 구분하였다.

동일한 모델 예측 결과와 임곗값에 행 단위 평가와 운영 환경 기반 평가를 각각 적용하여 Precision, Recall, FAR 및 Lead Time을 비교하였으며, 각 평가 기준에 따라 임곗값을 별도로 최적화하여 평가 기준과 모델 성능의 관계를 분석하였다. 그 결과 동일한 예측 결과를 사용하더라도 평가 절차와 임곗값 최적화 기준에 따라 성능지표와 모델 간 상대적 순위가 달라졌다. 데이터셋별 평가에서 두 방식의 1위 모델이 일치한 비율은 13~40%에 그쳤으며, 일부 데이터셋에서는 임곗값 기준의 변경만으로 30개 학습 seed 전체에서 1위 모델군이 달라졌다. 또한 운영 환경 기반 평가는 기존 분류 지표뿐만 아니라 최초 Alarm 발생 시점, Lead Time, 반복 Alarm 및 False Alarm 발생 시점과 같은 시간적·운영적 정보를 제공하였다.

이러한 결과는 HDD 고장 예측 모델의 성능을 개별 행의 분류 정확도만으로 평가하기보다, HDD별 시간적 관측 과정과 Alarm 발생 시점, 실제 고장과의 관계 및 관측 종료에 따른 중도절단을 함께 고려하여 평가할 필요가 있음을 보여준다.

주제어: HDD 고장 예측, SMART, 운영 환경 기반 평가, 행 단위 평가, 최초 Alarm, 우측 중도절단, Lead Time

## 1. 서론

데이터 저장 수요의 증가와 함께 HDD는 대규모 저장 시스템과 데이터센터에서 널리 활용되고 있다. HDD 고장은 데이터 손실과 서비스 가용성 저하로 이어질 수 있으므로, 고장을 사전에 탐지하고 유지보수 의사결정에 활용하기 위한 고장 예측 연구가 지속적으로 수행되어 왔다 [HDD 신뢰성·예지보전 연구]. 특히 SMART(Self-Monitoring, Analysis and Reporting Technology)는 HDD의 상태 및 오류 관련 속성을 모니터링하고 기록하는 기술이며, SMART 데이터는 HDD의 상태를 시점별로 관측할 수 있는 주요 정보원으로 활용되어 왔다 [HDD 통계·SMART 연구] [HDD 머신러닝 예측 연구]. 본 연구에서는 SMART 데이터를 각 HDD의 시간적 관측 이력을 구성하는 입력 정보로 사용한다.

그러나 HDD 고장 예측 모델의 성능은 어떤 평가 절차를 적용하는지에 따라 다르게 해석될 수 있다. 기존 평가에서는 개별 관측 행 또는 일정한 입력 Window의 예측 결과를 독립적인 분류 대상으로 취급하는 경우가 많다 [행·Window 단위 평가 관련]. 이러한 방식은 개별 예측의 분류 성능을 측정하는 데 유용하지만, 실제 운영에서 하나의 HDD에 대해 시간 순으로 반복되는 예측과 Alarm의 관계, 최초 Alarm 발생 시점, 실제 고장까지 확보되는 Lead Time 및 불필요한 Alarm 발생 특성을 충분히 반영하기 어렵다 [조기 시계열 분류 관련] [예지보전 평가 관련].

### 1.1 연구 배경 및 문제 제기

HDD의 SMART 데이터는 HDD별로 시간에 따라 반복적으로 수집되며, 실제 운영에서 유지보수와 교체의 대상이 되는 것은 개별 관측 행이 아니라 HDD 자체이다 [HDD 통계·SMART 연구] [HDD 교체·고장 예측 연구]. 따라서 동일 HDD에서 여러 시점에 발생한 예측 결과는 서로 완전히 독립적인 사건이라기보다, 하나의 HDD에 대한 시간적 경보 과정으로 해석될 수 있다.

그럼에도 기존 HDD 고장 예측 연구에서는 개별 Row 또는 특정 시점·Window의 예측 결과를 중심으로 Precision, Recall, F1-score, AUC 및 FAR 등을 산출하는 경우가 많다 [행·Window 단위 평가 관련]. 이 경우 하나의 HDD에서 반복적으로 발생한 Positive 예측이 여러 개의 개별 결과로 집계될 수 있으며, 동일 HDD에 대한 최초 Alarm과 후속 Alarm의 관계가 평가 결과에 직접 반영되지 않을 수 있다.

실제 운영에서는 단순히 고장을 한 번이라도 탐지했는지뿐만 아니라, 최초 Alarm이 언제 발생했는지, 고장 발생 전 충분한 대응 시간이 확보되었는지, Alarm이 반복적으로 발생했는지, 그리고 고장이 관측되지 않은 HDD에서 불필요한 Alarm이 발생했는지가 중요하다 [예지보전 평가 관련] [조기 시계열 분류 관련]. 특히 고장 전 너무 이른 시점에 발생한 Alarm은 고장과의 시간적 관계에 따라 실제 정비 의사결정에 유효한 Alarm으로 해석되지 않을 수 있으며, 관측 종료 전에 고장이 확인되지 않은 HDD는 이후 고장 가능성을 확인할 수 없으므로 별도의 처리가 필요하다.

따라서 HDD 고장 예측의 성능을 평가하기 위해서는 개별 관측 행의 분류 결과뿐만 아니라, 각 HDD의 관측 이력을 시간 순으로 처리하고 Alarm 발생과 실제 고장 또는 관측 종료 시점의 관계를 반영하는 평가 관점이 필요하다.

### 1.2 연구 목적 및 기여

본 연구의 목적은 HDD의 시간적 관측 과정을 반영한 운영 환경 기반 성능평가 방법을 제안하고, 기존 행 단위 평가와 비교하여 평가 절차에 따른 성능 차이를 분석하는 데 있다.

본 연구에서 '운영 환경 기반 평가'는 HDD의 시간적 관측 과정과 경보 발생 구조를 평가 절차에 반영하는 것을 의미하며, 구체적으로 ① 관측 이력의 시간 순 온라인 추론, ② 예측확률의 Alarm 변환, ③ HDD 단위 최초 Alarm 판정, ④ 우측 중도절단 구간의 처리 네 가지를 포함한다. 반면 실제 운영에서 함께 적용되는 경보 억제(alarm suppression), 최소 지속시간 및 재경보 대기(cooldown) 조건, 경보 단계화(escalation), 교체·점검 비용과 인력 운용 등의 대응 정책은 평가에 포함하지 않는다. 따라서 본 연구의 평가는 운영 정책 전반을 재현하는 것이 아니라, 모델의 예측이 HDD 단위 경보로 전환되기까지의 과정을 평가 대상으로 한다.

본 연구의 주요 내용은 다음과 같다.

- 첫째, 각 HDD의 SMART 관측값을 시간 순으로 입력하여 온라인 추론을 수행하고, 예측 확률이 의사결정 임곗값을 최초로 초과하는 시점을 최초 Alarm으로 정의한다. 이후 최초 Alarm과 실제 고장 시점 또는 관측 종료 시점의 관계를 이용하여 On-time, Early, Censored Early, Missed 및 Censored No Alarm을 구분한다.
- 둘째, 동일한 모델 예측 결과와 동일한 임곗값에 행 단위 평가와 운영 환경 기반 평가를 각각 적용하여, 평가 절차에 따른 Precision, Recall, FAR 및 Lead Time의 차이를 비교한다.
- 셋째, 행 단위 FAR 기준과 운영 환경 기반 FAR 기준으로 임곗값을 각각 최적화하고, 임곗값 선택 기준에 따라 성능지표와 모델 간 상대적 순위가 어떻게 달라지는지 분석한다.
- 넷째, 최초 Alarm 발생 시점, Lead Time, 반복 Alarm 및 False Alarm 발생 시점과 같은 정보를 분석하여, 행 단위 분류 성능만으로는 확인하기 어려운 HDD 운영 특성을 제시한다.

이를 통해 HDD 고장 예측 모델의 성능평가는 개별 행의 분류 정확도에만 한정하기보다, 실제 운영 과정에서의 Alarm 발생 시점과 고장 탐지의 유효성 및 대응 가능 시간을 함께 고려해야 함을 보이고자 한다.

---

## 2. 관련 연구

선행연구 정리

HDD 고장 예측 연구에서는 통계적·신뢰성 분석부터 머신러닝 및 딥러닝 기반의 고장·교체 예측까지 다양한 방법이 제안되어 왔으며, 연구마다 입력 데이터의 구성, 예측 대상, Prediction Horizon, 평가 절차 및 평가지표가 다르게 설정되어 왔다 [HDD 연구 전반]. 2.1에서는 예측 모델의 발전 과정을, 2.2에서는 평가 프로토콜의 차이와 운영 관점에서 제기되는 문제를 정리한다.

### 2.1 HDD SMART 기반 고장 예측 연구

HDD 고장 예측 연구는 대규모 운영 환경에서 수집된 고장 기록과 SMART 속성의 관계를 분석하는 통계적·신뢰성 분석에서 시작되었으며 [HDD 통계·SMART 연구], 이후 SMART 데이터로 고장 또는 교체 여부를 직접 예측하는 머신러닝 기반 이진 분류 연구로 확장되었다. Random Forest, Regularized Greedy Forest, SVM, XGBoost, LightGBM 등이 적용되었고, 주로 Precision, Recall, F1-score, AUC 또는 FAR과 같은 분류 성능지표가 사용되었다 [HDD 교체·고장 예측 연구] [행·Window 단위 평가 관련].

시계열 정보의 활용이 확대되면서 Sliding Window, LSTM, CNN-LSTM 및 Transformer와 같이 시간적 특성을 반영하는 모델도 제안되었다. 이러한 연구에서는 고장 전 일정 기간을 Prediction Horizon으로 설정하거나 일정 길이의 과거 관측 구간을 입력으로 사용하여 고장 여부 또는 잔여수명을 예측하였다 [HDD 시계열·딥러닝 연구] [RUL 예측 연구]. 다만 많은 경우 평가 대상은 개별 Row 또는 Window이며, HDD 전체 생애에 걸친 연속적 추론과 최초 Alarm을 기준으로 한 운영 평가는 별도로 수행되지 않았다 [행·Window 단위 평가 관련].

최근에는 클래스 불균형, Concept Drift, 온라인 학습, 도메인 적응 및 데이터 품질을 고려하는 연구가 확장되고 있으며, 연구 목적 또한 잔여수명 예측, 조기 고장 탐지 및 설명 가능한 예측으로 넓어지고 있다 [클래스 불균형 연구] [Concept Drift·온라인 학습 연구] [RUL 예측 연구] [조기 고장 탐지 연구]. 다만 온라인 스트림과 Concept Drift를 다루는 연구에서도 온라인 학습 또는 온라인 추론의 적용 자체가 최초 Alarm과 우측 중도절단을 포함한 운영 성능평가를 의미하지는 않는다 [Concept Drift·온라인 학습 연구].

이처럼 선행 연구는 서로 다른 데이터셋, 모델 구조, 입력 방식 및 레이블 정의를 사용하며 예측 결과를 평가하는 대상과 절차도 다양하게 나타난다. 따라서 HDD 고장 예측 연구의 발전은 모델 구조의 복잡화뿐만 아니라 예측 결과를 어떤 대상과 기준으로 평가할 것인가의 문제로도 이어진다. 본 연구는 모델 구조의 개선보다, 동일한 예측 결과를 운영 과정에 적용했을 때 성능이 어떻게 해석되는지를 평가하는 절차에 초점을 둔다.

### 2.2 HDD 고장 예측의 평가와 운영 관점 연구

HDD 고장 예측 연구에서는 개별 Row, Window, 특정 시점의 HDD 또는 HDD 전체 이력 등 서로 다른 평가 대상이 사용되어 왔다. 또한 Prediction Horizon의 길이, 양성 레이블의 정의, Alarm 발생 기준 및 평가 종료 조건도 연구마다 다르게 설정되어 있다 [평가 절차 비교 연구].

**표 1. 선행 연구의 평가 프로토콜 비교**

| 연구 | 예측 단위 | 평가 단위 | 평가 데이터 범위 | Prediction Horizon | 주요 지표 | [링크], (참고용) |
| --- | --- | --- | --- | --- | --- | --- |
| Predicting Disk Replacement towards Reliable Data Centers (2016) | 행 | 행 | 고장 전 일부 구간 | 10–15일 | Precision, Recall, F1 | https://dl.acm.org/doi/epdf/10.1145/2939672.2939699 |
| Robust Data Preprocessing for Machine-Learning-Based Disk Failure Prediction in Cloud Production Environments (2019) | 행 | HDD | 전체(시간 구간 분할) | 고정값 없음 | TPR, FPR | https://arxiv.org/pdf/1912.09722 |
| Making Disk Failure Predictions SMARTer! (2020) | Sequence | HDD | 전체 | 10일 | Precision, Recall, F1, MCC | https://www.usenix.org/system/files/fast20-lu.pdf |
| PAKDD2020 Alibaba AI Ops Competition: Large-Scale Disk Failure Prediction (2020) | 행 | 행 | 전체 | 30일 | Precision, Recall, F1 | https://link.springer.com/chapter/10.1007/978-981-15-7749-9_7 |
| A disk failure prediction method based on LSTM network due to its individual specificity (2020) | Window | Window | 명시 없음 | 15일 | FDR, FAR, Precision, F1 | https://www.sciencedirect.com/science/article/pii/S1877050920319700?fr=RR-2&ref=pdf_download&rr=a2b8db927babf804 |
| Hard Disk Failure Prediction Based on LightGBM with CID (2021) | 행/Window | HDD | 전체 | 10일 | AUC, F1, TPR, FPR, DPF | https://ieeexplore-ieee-org-ssl.openlink.mju.ac.kr/stamp/stamp.jsp?tp=&arnumber=9631504 |
| Machine Learning Model Update Strategies for Hard Disk Drive Failure Prediction (2021) | 행 | 행 | 전체 | 10일 | FDR, FAR | https://ieeexplore-ieee-org-ssl.openlink.mju.ac.kr/stamp/stamp.jsp?tp=&arnumber=9680247 |
| StreamDFP: A General Stream Mining Framework for Adaptive Disk Failure Prediction (2023) | 행 | 행 | 전체 | 30일 | Precision, Recall, F1, Days in Advance | https://ieeexplore-ieee-org-ssl.openlink.mju.ac.kr/stamp/stamp.jsp?tp=&arnumber=9737380 |
| Leveraging survival analysis in cost-aware deepnet for efficient hard drive failure prediction (2024) | 행 | 행 | 전체 | 15일 | G-mean, FDR, FAR | https://link.springer.com/article/10.1007/s00521-024-10479-6?utm_source |
| DFPoLD: A Hard Disk Failure Prediction on Low-Quality Datasets (2025) | 행 | 행 | 고장·정상 HDD의 일부 구간 | 결측률에 따라 결정§ | TPR, FPR, AUC, F1, DPF | https://www.mdpi.com/2227-9709/12/3/73?utm_source |
| Explainable time series features for hard disk drive failure prediction (2025) | Segment | HDD | HDD당 1개 구간(마지막 32일) | 5일 | FDR, FAR | https://www.sciencedirect.com/science/article/pii/S0952197625006748?ref=pdf_download&fr=RR-2&rr=a2b8dc14aec8f804 |
| A Hybrid TCN-LSTM-Attention Network for Disk Failure Prediction (2026) | Window | Window | 균형화된 30일 Window 표본 | 7일 | Precision, F1, FDR, FAR | https://ieeexplore-ieee-org-ssl.openlink.mju.ac.kr/stamp/stamp.jsp?tp=&arnumber=11572580 |
| 본 연구 | 행 | 행 / HDD | 전체 | 30일 | HDD-level Precision, Recall, FAR, Median Lead Time |  |

여기서 예측 단위는 하나의 예측값이 산출되는 단위이며, 평가 단위는 성능지표가 최종적으로 집계되는 단위이다. 평가 데이터 범위의 '전체'는 각 HDD의 관측 이력을 모두 평가에 사용함을 의미하며, 데이터 수집 기간의 길이는 연구마다 다르다(예: Lu et al. 약 70일, 본 연구 최대 약 2,500일).

행 연구 중 일부는 Prediction Horizon을 고정된 단일 값으로 설정하지 않는다. RODMAN은 변화점 탐지를 통해 고장 이전 구간의 길이를 데이터로부터 결정하며, DFPoLD는 결측률에 따라 고장 전 양성 레이블링을 위한 시간 구간을 결정한다. 또한 StreamDFP는 미래 30일 이내의 고장을 예측하는 문제를 다루면서 고장 전 20일을 양성 레이블링 기간으로 사용한다. 따라서 본 연구에서 사용하는 Prediction Horizon 30일과 선행 연구의 30일 또는 고장 전 레이블링 기간은 동일한 개념으로 단순 비교할 수 없다. 

**설명 가능한 시계열 특징**의 5일 Lead Time은 입력 구간의 종료 시점과 고장 시점 사이의 간격으로 정의되므로, 본 연구의 최초 Alarm 시점과 고장 시점 사이의 Lead Time과는 정의가 다르다.

이처럼 선행 연구의 평가 프로토콜은 예측 단위, 평가 단위, 평가 데이터의 범위에서 서로 차이를 보이며, 특히 예측 단위와 평가 단위가 반드시 일치하지는 않는다. 평가 데이터의 범위 또한 관측 이력 전체를 사용하는 연구부터 고장 전 일정 구간이나 균형 표본만을 사용하는 연구까지 나뉜다.

본 연구는 이 가운데 각 관측 행을 독립적인 분류 대상으로 집계하는 방식과 HDD 전체 관측 이력을 사용하는 구성을 기준선으로 채택한다. 이는 특정 연구의 설정을 임의로 선택한 것이 아니라, 표 1에서 확인할 수 있듯이 관측 이력 전체에 행 단위 평가를 적용하는 구성이 선행 연구에서 복수의 사례로 사용되어 온 프로토콜이기 때문이다. 평가 데이터의 범위를 운영 환경 기반 평가와 동일하게 고정한 추가적인 이유는 4.2절에 기술한다.

또한 평가 데이터의 범위는 두 평가에서 동일하게 고정하였다(4.2절). 이 구성에서는 행 단위 Precision이 고장 전 일정 구간만을 평가하거나 HDD별로 표본을 추출하여 평가한 선행 연구의 보고값보다 낮게 나타날 수 있으므로, 본 연구는 행 단위 지표의 절대적 수준이 아니라 동일한 행 집합에서 평가 절차만을 달리했을 때의 상대적 차이에 초점을 둔다.

일부 연구에서는 FAR, Lead Time, Days in Advance 또는 DPF와 같은 운영 관련 지표를 활용하고, 시간 순으로 데이터를 처리하거나 HDD의 전체 생애에 해당하는 데이터를 사용하기도 한다 [운영 지표 활용 연구]. 그러나 운영 관련 지표를 사용한다는 사실과 HDD의 운영 단위에서 모델을 평가한다는 것은 동일하지 않다. 각 시점의 예측을 독립적인 분류 대상으로 취급하여 FAR이나 Recall을 계산하면 동일 HDD에서 반복적으로 발생한 Positive 예측이 여러 개의 결과로 분산 집계되므로, 최초 Alarm이 발생한 시점과 이후 Alarm의 지속·반복 여부가 평가 결과에 직접 반영되지 않는다 [행 단위 평가 관련].

반면 실제 운영에서 하나의 HDD에 대한 여러 시점의 예측은 하나의 경보 과정으로 해석되며, 최초 Alarm이 언제 발생했는지, 실제 고장까지 어느 정도의 대응 시간이 확보되었는지, 고장이 관측되지 않은 HDD에서 불필요한 Alarm이 얼마나 발생했는지를 함께 고려해야 한다 [예지보전 평가 관련] [조기 시계열 분류 관련]. 따라서 행 단위 평가에서 높은 성능을 보인 모델이 HDD 운영 단위에서도 같은 수준의 성능을 보인다고 단정하기 어렵다 [운영 평가 관련].

한편 예지보전과 시계열 기반 의료 AI 등 연속적인 예측이 수행되는 분야에서도 개별 예측의 정확성과 개체·운영 단위의 성능이 서로 다를 수 있음이 보고되고 있으며, 중복 예측과 Alarm을 실제 사건 및 개체 단위의 결과와 연결하여 평가하는 방법이 제안되고 있다 [예지보전 평가 연구] [연속 예측 평가 관련]. 조기 시계열 분류 연구 또한 예측의 정확성과 의사결정의 조기성 사이의 trade-off를 주요 문제로 다룬다 [조기 시계열 분류 관련]. 이러한 연구들은 HDD 고장 예측에 직접 적용되는 평가 방법을 제시하지는 않으나, 개별 시점의 예측 성능과 운영 단위의 성능을 구분하고 예측 시점과 반복 Alarm을 평가에 포함할 필요성을 뒷받침한다.

따라서 HDD 고장 예측의 운영 성능을 평가하기 위해서는 시간 순으로 수행되는 예측을 Alarm으로 변환하고, HDD별 최초 Alarm과 실제 고장 또는 관측 종료 시점의 관계를 평가 절차에 반영할 필요가 있다. 본 연구는 이러한 절차를 적용하여 기존 행 단위 평가와 운영 환경 기반 평가의 결과를 동일한 예측 결과에서 비교한다.

**연구 공백**

기존 HDD 고장 예측 연구에서는 SMART 기반 통계 분석, 머신러닝·딥러닝 기반 고장 예측, 온라인 학습, RUL 예측 및 FAR·Lead Time과 같은 운영 관련 지표가 각각 연구되어 왔다 [HDD 연구 전반] [운영 지표 활용 연구]. 그러나 저자가 확인한 범위에서, HDD 전체 관측 이력에 대해 시간 순으로 온라인 추론을 수행하고, 최초 Alarm, Lead Time, 우측 중도절단 및 HDD-level 지표를 하나의 평가 절차로 통합하여 모델의 운영 성능을 비교한 연구는 제한적이다 [HDD 평가 절차 비교].

따라서 본 연구에서는 HDD의 시간적 운영 과정을 반영한 운영 환경 기반 성능평가 절차를 제안한다. 제안한 절차에서는 각 HDD의 관측 이력을 시간 순으로 처리하여 Alarm을 생성하고, 최초 Alarm과 실제 고장 또는 관측 종료 시점의 관계를 이용하여 On-time, Early, Censored Early, Missed 및 Censored No Alarm을 구분한다. 또한 동일한 예측 결과에 기존 행 단위 평가와 운영 환경 기반 평가를 각각 적용하여, 평가 절차에 따른 성능지표와 모델 순위의 차이를 분석한다.

---

## 3. 평가 방법

본 장에서는 본 연구에서 사용하는 두 가지 평가 방법을 정의한다. 3.1의 행 단위 평가는 각 관측 행을 독립적인 분류 대상으로 취급하는 방식으로, 선행 연구에서 널리 사용되어 온 평가 절차이자 본 연구의 기준선에 해당한다. 3.2의 운영 환경 기반 평가는 각 HDD의 관측 이력을 시간 순으로 유지하고 최초 Alarm을 기준으로 HDD 단위의 성능을 산출하는 본 연구의 제안 방법이다. 두 평가는 동일한 온라인 추론 결과와 동일한 평가 대상 행 집합을 사용하며, 그 결과를 판정하고 집계하는 절차에서만 차이가 있다. 따라서 두 평가에서 관찰되는 성능 차이는 예측 결과나 평가 대상의 차이가 아니라 평가 절차의 차이에 귀속된다. 두 평가의 전체 구조는 그림 1에 제시하였다.

[그림 1. 행 단위 평가와 운영 환경 기반 평가의 전체 구조]

### 3.1 행 단위 평가

행 단위 평가는 각 HDD의 개별 관측 행을 독립적인 분류 대상으로 취급하는 평가 방식이다. HDD $d$의 관측 시점 $t$에 대한 예측확률 $p_{d,t}$가 의사결정 임곗값 $\tau$ 이상인 경우 해당 행을 양성으로 예측한 것으로 보고, 각 행의 예측 결과와 레이블을 대조하여 True Positive, False Positive, False Negative 및 True Negative를 집계한다. 이때 각 행은 서로 독립적인 분류 결과로 취급되므로, 동일한 HDD에서 여러 시점에 발생한 양성 예측은 서로 구분되지 않고 개별 결과로 합산된다. 이러한 절차는 선행 연구에서 널리 사용되어 온 평가 방식에 해당하며(2.2절), 본 연구에서는 이를 제안 방법과 비교하기 위한 기준선으로 사용한다.

행 단위 레이블은 다음과 같이 정의한다. 고장 시점이 관측된 HDD에서 실제 고장까지 남은 기간이 Prediction Horizon 이내인 행, 즉 $t_{\mathrm{failure},d}-t \le H$인 행을 양성으로 정의하고 그 외의 행을 음성으로 정의한다. 우측 중도절단 HDD는 관측 종료 이후의 고장 여부를 확인할 수 없으므로 모든 행을 음성으로 처리하며, 잔여 관측 기간이 $H$일 미만인 행($t_{\mathrm{end},d}-t<H$)은 레이블을 확정할 수 없으므로 평가에서 제외한다. 이 제외 기준은 3.2.1에서 정의하는 운영 환경 기반 평가의 평가 구간 정의와 동일하며, 두 평가는 동일한 Prediction Horizon $H$를 사용한다.

$TP$, $FP$, $FN$, $TN$을 위 레이블 기준의 행 단위 집계값이라 할 때, 행 단위 성능지표는 다음과 같이 정의한다.

$$
\mathrm{Precision}_{\mathrm{row}}
=
\frac{TP}{TP+FP}
\qquad
\mathrm{Recall}_{\mathrm{row}}
=
\frac{TP}{TP+FN}
\qquad
\mathrm{FAR}_{\mathrm{row}}
=
\frac{FP}{FP+TN}
$$

Precision의 분모는 양성으로 예측된 행 전체, Recall의 분모는 양성 레이블을 갖는 행 전체이며, FAR의 분모는 음성 레이블을 갖는 행 전체이다. 즉 행 단위 FAR은 음성 행 가운데 양성으로 잘못 예측된 행의 비율을 의미하며, 동일한 HDD에서 반복적으로 발생한 오탐은 각각 별개의 행으로 집계된다.

평가 대상은 test 분할에 포함된 HDD의 전체 관측 이력 중 위 기준으로 제외되지 않은 모든 행이다. 시계열 모델은 일정 길이의 과거 관측 구간을 입력으로 사용하므로 관측 이력의 초반부에서는 입력 구간을 온전히 확보할 수 없으나, 본 연구에서는 관측 이력의 첫 행을 반복하여 입력 구간을 채우는 방식으로 처리하였다. 따라서 네 모델 모두 동일한 행에서 예측확률이 산출된다.

### 3.2 운영 환경 기반 평가

본 연구에서는 HDD별 시간 순서에 따라 온라인 추론을 수행하고, 각 시점의 예측 확률을 임곗값에 따라 Alarm으로 변환한다. 하나의 HDD에서는 시간에 따라 여러 차례 Alarm이 발생할 수 있으므로, 동일 HDD에서 발생한 반복 Alarm을 하나의 운영 사건으로 통합하고 **최초 Alarm**을 기준으로 해당 HDD의 경보 결과를 판정한다. 이후 최초 Alarm과 실제 고장 시점의 시간적 관계를 이용하여 고장 이전의 적시 탐지 여부와 Lead Time을 평가하고, 그 결과를 HDD 단위로 집계하여 모델의 운영 환경 기반 성능을 산출한다.

이러한 평가 방식은 개별 관측 시점의 예측 정확성을 평가하는 행 단위 평가와 구분된다. 행 단위 평가에서는 각 관측 행이 독립적인 분류 결과로 취급되지만, 실제 운영에서는 동일 HDD에 대한 여러 시점의 예측이 하나의 연속적인 상태 판단 과정으로 이루어진다. 따라서 모델이 각 HDD에 대해 언제 처음 경보를 발생시켰으며, 해당 경보가 실제 고장에 대해 어느 정도의 사전 대응 시간을 제공했는지를 평가하기 위해서는 HDD의 시간 순서를 보존한 평가가 필요하다.

본 연구의 운영 환경 기반 평가는 **온라인 추론 → Alarm 생성 → 최초 Alarm 판정 → 고장과의 시간적 관계에 따른 결과 분류 → HDD 단위 성능 집계**의 순서로 수행된다. 또한 관측 기간 내 고장 시점이 확인된 HDD와 관측 종료 시점까지 고장이 확인되지 않은 우측 중도절단 HDD를 구분하여 평가한다. 전체 평가 절차는 그림 1에 제시하였다.

> 
> 
> 
> **그림 1.** 행 단위 평가와 운영 환경 기반 평가의 전체 구조. 두 평가는 동일한 온라인 추론 결과와 동일한 평가 대상 행 집합에서 출발하며, 행 단위 평가는 각 행을 독립적인 분류 대상으로 집계하고 운영 환경 기반 평가는 Alarm 변환과 HDD별 최초 Alarm 판정을 거쳐 HDD 단위로 집계한다.
> 

#### 3.2.1 평가 대상 및 판정 기준

**평가 대상**

본 연구에서는 HDD를 $d$, HDD $d$의 관측 시점 집합을 $T_d$로 표기하고, 각 HDD의 SMART 관측값을 시간 순으로 정렬하여 운영 이력으로 구성한다. 평가 대상 HDD는 관측 기간 내 고장 시점이 확인된 HDD와 관측 종료 시점까지 고장이 확인되지 않은 우측 중도절단 HDD로 구분한다. 여기서 우측 중도절단은 HDD가 정상임을 의미하는 것이 아니라, 관측 종료 이후의 고장 여부와 고장 시점을 확인할 수 없음을 의미한다.

각 모델은 시간 순서에 따라 각 관측 시점의 고장 예측 확률  $p_{d,t}$를 산출하며, $p_{d,t}$가 임곗값 $\tau$ 이상인 시점에서 Alarm이 발생한 것으로 정의한다. 동일한 HDD에서는 여러 시점에 Alarm이 반복하여 발생할 수 있으므로, HDD 단위의 평가에서는 평가 대상 구간에서 **최초로 발생한 Alarm**을 기준으로 판정한다. 이를 HDD $d$의 최초 Alarm 시점 $t_{\mathrm{alarm},d}$로 표기하며, 평가 대상 구간에서 Alarm이 발생하지 않은 HDD는 최초 Alarm이 없는 것으로 처리한다.

**Lead Time과 Prediction Horizon**

고장 시점이 관측된 HDD의 실제 고장 시점을 $t_{\mathrm{failure},d}$라 할 때, 최초 Alarm이 존재하는 경우 Lead Time은 $LT_d=t_{\mathrm{failure},d}-t_{\mathrm{alarm},d}$로 정의한다. HDD의 관측은 고장 시점까지 이루어지므로 $LT_d$는 음수가 될 수 없다.

세그먼트가 분리된 HDD도 운영 평가에서는 하나의 HDD로 취급하며, Lead Time은 관측 공백을 포함한 달력일 기준으로 산출한다.

본 연구에서는 Prediction Horizon을 $H=30$일로 설정한다. 최초 Alarm의 Lead Time이 $H$ 이하인 경우 **On-time**, $H$를 초과하는 경우 **Early**로 분류한다. 고장 시점이 관측된 HDD에서 최초 Alarm이 발생하지 않은 경우 **Missed**로 분류한다.

**우측 중도절단 HDD의 처리**

우측 중도절단 HDD는 관측 종료 시점 $t_{\mathrm{end},d}$ 이후의 고장 여부를 확인할 수 없으므로, 관측 종료 직전 $H$일 동안에는 해당 Alarm이 On-time인지 여부를 판단할 수 없다. 따라서 본 연구에서는 **관측 종료 시점으로부터 $H$일에 해당하는 마지막 구간을 평가에서 제외**하고, 그 이전에 발생한 최초 Alarm만을 평가 대상으로 한다.

평가 대상 구간에서 최초 Alarm이 발생한 우측 중도절단 HDD의 경우 실제 고장 시점은 관측 종료 이후에 존재할 가능성이 있으므로 정확한 Lead Time은 산출할 수 없다. 그러나 해당 Alarm이 관측 종료 시점보다 최소 $H$일 이전에 발생하였으므로, 향후 고장이 발생한다면 그 Lead Time은 $H$를 초과하게 된다. 이를 다음과 같이 나타낼 수 있다.

$$
LT_d
=
t_{\mathrm{failure},d}-t_{\mathrm{alarm},d}
>
t_{\mathrm{end},d}-t_{\mathrm{alarm},d}
\geq H
$$

즉, 실제 고장 시점이 관측되지 않았더라도 해당 Alarm은 **On-time으로 분류될 수 없음이 보장**된다. 따라서 이를 **Censored Early**로 분류하며, 평가 대상 구간에서 최초 Alarm이 발생하지 않은 우측 중도절단 HDD는 **Censored No Alarm**으로 분류한다.

이러한 판정 절차를 통해 각 HDD는 **On-time, Early, Missed, Censored Early, Censored No Alarm**의 다섯 가지 결과 중 하나로 분류된다.

!**그림 2.** 고장 관측 HDD와 우측 중도절단 HDD의 평가 구간 및 결과 범주($H=30$일). 고장 관측 HDD는 고장 시점 $t_{\mathrm{failure},d}$로부터 $H$일 이내의 최초 Alarm을 On-time, 그보다 이른 최초 Alarm을 Early, 최초 Alarm이 없는 경우를 Missed로 분류한다. 우측 중도절단 HDD는 관측 종료 시점 $t_{\mathrm{end},d}$에서 $H$일을 뺀 시점까지를 평가 구간으로 하며, 구간 내 최초 Alarm의 유무에 따라 Censored Early와 Censored No Alarm으로 분류한다. 점선은 모든 HDD에 공통으로 적용되는 관측 종료 시점이다.

**그림 2.** 고장 관측 HDD와 우측 중도절단 HDD의 평가 구간 및 결과 범주($H=30$일). 고장 관측 HDD는 고장 시점 $t_{\mathrm{failure},d}$로부터 $H$일 이내의 최초 Alarm을 On-time, 그보다 이른 최초 Alarm을 Early, 최초 Alarm이 없는 경우를 Missed로 분류한다. 우측 중도절단 HDD는 관측 종료 시점 $t_{\mathrm{end},d}$에서 $H$일을 뺀 시점까지를 평가 구간으로 하며, 구간 내 최초 Alarm의 유무에 따라 Censored Early와 Censored No Alarm으로 분류한다. 점선은 모든 HDD에 공통으로 적용되는 관측 종료 시점이다.

#### 3.2.2 운영 성능평가 지표

3.2.1에서 정의한 HDD별 최초 Alarm의 판정 결과를 이용하여 모델의 운영 환경 기반 성능을 평가한다. 본 연구에서는 HDD-level Precision, HDD-level Recall, HDD-level FAR 및 Median Lead Time을 사용하며, 각 지표는 Alarm 발생 여부와 그 시간적 위치를 HDD 단위에서 평가한다. 판정 결과가 $X$인 HDD의 집합을 $D_X$, 그 수를 $N_X$로 표기한다.

HDD-level Precision은 최초 Alarm이 발생한 HDD 중 **On-time으로 판정된 HDD의 비율**로 정의한다.

$$
\mathrm{Precision}_{\mathrm{HDD}}
=
\frac{N_{\mathrm{On\text{-}time}}}
{N_{\mathrm{On\text{-}time}}
+
N_{\mathrm{Early}}
+
N_{\mathrm{Censored\ Early}}}
$$

분모는 평가 대상 구간에서 최초 Alarm이 발생한 HDD 전체이며 Early와 Censored Early는 운영상 적절한 사전경보로 인정하지 않으므로, 이 지표는 Alarm 발생 빈도가 아니라 발생한 Alarm이 대응 가능한 시점에 발생했는지를 평가한다.

HDD-level Recall은 **고장 시점이 관측된 HDD 중 Prediction Horizon 이내에 최초 Alarm이 발생한 HDD의 비율**로 정의한다.

$$
\mathrm{Recall}_{\mathrm{HDD}}
=
\frac{N_{\mathrm{On\text{-}time}}}
{N_{\mathrm{On\text{-}time}}
+
N_{\mathrm{Early}}
+
N_{\mathrm{Missed}}}
$$

최초 Alarm이 발생하였더라도 Lead Time이 $H$를 초과하면 적시 탐지로 인정하지 않으므로, 이 지표는 고장 전에 Alarm이 발생했는지만이 아니라 운영상 활용 가능한 Prediction Horizon 이내에 탐지했는지를 평가한다.

HDD-level FAR은 **관측 종료 시점까지 고장이 확인되지 않은 우측 중도절단 HDD 중 최초 Alarm이 발생한 HDD의 비율**로 정의한다.

$$
\mathrm{FAR}_{\mathrm{HDD}}
=
\frac{N_{\mathrm{Censored\ Early}}}
{N_{\mathrm{Censored\ Early}}
+
N_{\mathrm{Censored\ No\ Alarm}}}
$$

이는 개별 관측 행의 오탐 비율을 계산하는 행 단위 FAR과 달리 HDD 자체를 기준으로 불필요한 Alarm이 발생한 비율을 나타내며, 동일 HDD에서 Alarm이 여러 차례 반복되더라도 하나로 집계한다. 이때 우측 중도절단 HDD의 Alarm을 오탐으로 세는 것은 해당 HDD가 향후 고장하지 않는다고 가정하는 것이 아니라, 평가 구간의 정의에 따라 그 Alarm 이후 최소 $H$일 동안 고장이 관측되지 않았음이 보장되어 관측된 범위 내에서 On-time 조건을 충족하지 못함을 의미한다. 이 지표는 평가 구간 전체에서 최초 Alarm의 발생 여부를 집계하므로 관측 기간이 길수록 값이 커지며, 관측 기간이 다른 데이터셋 간의 절대 수준 비교에는 적합하지 않다.

Median Lead Time은 최초 Alarm이 발생한 **고장 관측 HDD**의 Lead Time 중앙값으로 정의한다.

$$
\mathrm{Median\ Lead\ Time}
=
\operatorname{median}
\left\{
LT_d
\mid
d\in
D_{\mathrm{On\text{-}time}}
\cup
D_{\mathrm{Early}}
\right\}
$$

On-time과 Early는 모두 최초 Alarm이 발생한 고장 관측 HDD이므로 함께 포함하며, 실제 고장 시점을 확인할 수 없는 우측 중도절단 HDD는 제외한다. 지나치게 긴 Lead Time은 Early Alarm의 증가를 의미할 수 있으므로, 이 지표는 클수록 좋은 단조적 성능 지표가 아니라 Alarm이 실제 고장 시점으로부터 어느 정도의 시간적 여유를 두고 발생하는지를 나타내는 운영 특성 지표로 활용한다.

---

## 4. 실험 설계

본 장에서는 3장에서 정의한 두 평가 방법을 동일한 예측 결과에 적용하기 위한 실험 조건을 기술한다. 4.1에서는 사용한 데이터와 전처리, 레이블 정의 및 데이터 분할, 그리고 비교 대상 모델과 학습 설정을 제시한다. 4.2에서는 두 평가가 각각의 기준으로 의사결정 임곗값을 선정하는 절차와 평가 대상 행 집합의 통제 방식, 그리고 전체 실험 구성을 제시한다.

본 연구의 관심은 모델 구조의 개선이 아니라 평가 절차와 임곗값 선택 기준이 성능 해석에 미치는 영향이므로, 실험은 이 두 요인을 제외한 조건을 모두 고정하도록 구성하였다. 네 모델은 동일한 전처리와 동일한 분할에서 학습되며, 특정 모델에 유리한 조정이 개입하지 않도록 하이퍼파라미터는 널리 사용되는 기본값으로 통일하고 규제 수준을 네 모델에서 대등하게 맞추었다. 

데이터 분할은 하나의 고정된 난수로 한 번만 수행하여 평가 대상 HDD 집합을 모든 실험에서 동일하게 유지하고, 학습의 무작위성만을 30개 random seed로 변화시켰다. 이러한 구성에서 관찰되는 차이는 변경한 요인, 즉 평가 방식 또는 임곗값 기준에 귀속된다.

### 4.1 데이터 및 실험 설정

본 연구는 Backblaze가 공개하는 데이터센터 HDD의 SMART 기록을 사용하였다. 서로 다른 제조사의 HDD 모델 3종(Seagate ST12000NM0007, Toshiba MG07ACA14TA, HGST HUH721212ALN604)을 선정하여 특정 모델의 특성에 결과가 지나치게 의존하는지를 확인할 수 있도록 하였다. 세 모델은 HDD 수, 고장률, 관측 기간이 서로 달라 데이터 특성이 다른 조건에서 평가 결과를 비교할 수 있다(표 3).

HDD 모델 선택

하드 디스크 드라이브 모델 선정 기준

**표 2. 실험에 사용한 HDD 모델별 데이터 특성 (전처리 후 기준)**

| HDD 모델 | 관측 기간 | HDD 수 | 고장 관측 HDD 수(비율) | 전체 행 수 | 양성 레이블 비율 | 관측일수 중앙값(IQR) |
| --- | --- | --- | --- | --- | --- | --- |
| ST12000NM0007 | 2017.09–2026.03 | 38,790 | 2,236 (5.8%) | 37,331,086 | 0.181% | 962 (772–1,141) |
| MG07ACA14TA | 2018.06–2026.03 | 39,351 | 2,076 (5.3%) | 75,086,961 | 0.083% | 1,933 (1,772–2,065) |
| HUH721212ALN604 | 2018.07–2026.03 | 11,370 | 1,608 (14.1%) | 26,863,127 | 0.176% | 2,512 (2,423–2,587) |

† 전처리 이후 기준이다. 양성 레이블은 고장 관측 HDD에서 실제 고장까지 남은 기간이 H일 이내인 행이며, 3.2.1절의 평가 구간 정의에 따라 학습·평가에서 제외되는 우측 중도절단 HDD의 말단 H일은 반영하지 않은 값이다.

모델 간 비교의 공정성을 위해 세 데이터셋에 동일한 전처리를 적용하였다. Normalization 컬럼과 분석에 불필요한 메타데이터 컬럼, 결측치 비율이 90%를 초과하는 컬럼을 제거하고 중복 행을 제거하였다. 관측일 사이의 공백은 3일 이하인 경우 결측일의 행을 생성한 뒤 직전 값으로 보간하고, 4일 이상인 경우 별도의 시계열 세그먼트로 분리하였다. 이후 남은 결측치를 포함한 행과 고장으로 기록된 뒤 다시 정상으로 기록된 HDD를 제거하였다. 

세그먼트 분리는 시간적으로 불연속인 구간이 하나의 입력 윈도우에 섞이지 않도록 하기 위한 처리이며, HUH721212ALN604 기준 11,370대 중 813대(7.1%)에 적용되었다. 시계열 모델의 입력 윈도우는 각 세그먼트 내부에서 구성하되, 평가에서는 세그먼트가 분리된 HDD도 하나의 HDD로 취급하여 최초 Alarm을 판정하며 Lead Time은 달력일 기준으로 산출한다. 

전처리 과정에서 HDD 수는 ST12000NM0007 38,843대에서 38,790대로, MG07ACA14TA 39,387대에서 39,351대로, HUH721212ALN604 11,438대에서 11,370대로 감소하였다. 전처리 결과 사용된 특성은 데이터셋에 따라 18~26개이다.

**레이블과 데이터 분할**

HDD 고장 예측을 이진 분류 문제로 정의하고, 실제 고장일 이전 $H=30$일에 해당하는 관측 시점을 양성으로 두었다. 우측 중도절단 HDD는 관측 종료 이후의 고장 여부를 확인할 수 없으므로, 3.2.1절의 평가 구간 정의와 동일하게 잔여 관측 기간이 $H$일 미만인 행($t_{\mathrm{end},d}-t<H$)을 학습·검증·테스트 데이터에서 모두 제외하였다.

데이터는 HDD 단위 정보 누출을 방지하기 위해 HDD의 고장 관측 여부로 층화한 그룹 분할을 적용하여 8:1:1로 나누었으며, 동일 HDD가 두 개 이상의 분할에 포함되지 않도록 하였다. 따라서 모든 평가는 학습에 사용되지 않은 HDD에 대해 수행된다. 데이터 분할은 하나의 고정된 난수로 한 번만 수행하였으므로, 후술하는 30개 random seed는 모델 학습의 무작위성만을 변화시키며 평가 대상 HDD 집합은 모든 실험에서 동일하다.

이러한 분할은 특정 시점 이후의 고장을 예측하는 시간 외삽 성능이 아니라, 학습에 사용되지 않은 HDD에 대한 일반화 성능을 평가 대상으로 삼기 때문이다. 각 테스트 HDD에 대해서는 관측 이력 전체를 시간 순으로 하루씩 진행하며 추론하므로, HDD 단위의 정보 누출을 차단하면서도 HDD 내부의 시간 순서는 그대로 보존된다. 다만 이 구성에서 학습과 테스트는 동일한 달력 구간을 공유하므로 수집 기간에 따른 고장률 변화가 성능 수준에 반영될 수 있다. 본 연구의 비교는 동일한 예측 결과에 서로 다른 평가 절차를 적용한 상대적 차이에 근거하므로, 이 요인은 두 평가에 동일하게 작용한다.

**모델과 학습**

트리 기반 모델(LightGBM, XGBoost)과 시계열 모델(LSTM, GRU)을 비교 대상으로 선정하고, 모델 간 비교가 학습 조건의 차이에 좌우되지 않도록 널리 사용되는 기본 하이퍼파라미터를 적용하되 규제 수준을 네 모델에서 대등하게 맞추었다(표 4).

시계열 모델에 한해 입력 피처를 표준화하였으며, 정보 누출을 방지하기 위해 학습 데이터에서만 StandardScaler를 적합한 뒤 검증·테스트 데이터에 동일하게 적용하고, 표준화 이후의 극단값 영향을 완화하기 위해 [-10,10]으로 클리핑하였다. 트리 기반 모델은 원본 피처를 그대로 사용하였다.

과적합을 방지하기 위해 검증 성능을 기준으로 Early Stopping을 적용하고 검증 성능이 가장 좋은 시점의 가중치를 사용하였다. 감시 지표는 각 모델 계열에서 표준적으로 사용되는 기준을 적용하여, 부스팅 라운드 단위로 갱신되는 트리 기반 모델에는 소수 클래스의 순위화 성능을 직접 반영하는 지표(LightGBM은 Average Precision, XGBoost는 AUC-PR)를, epoch 단위로 갱신되는 시계열 모델에는 검증 손실을 사용하였다. 네 모델 모두 표 4의 반복 상한에 도달하기 전에 조기 종료되었으므로, 반복 상한의 설정 차이가 모델 간 비교에 영향을 주지 않는다. 학습 손실은 네 모델 모두 이진 교차 엔트로피를 사용하였다.

**표 3. 모델별 학습 설정.** 반복과 Early Stopping의 단위는 모델 계열에 따라 다르며, 트리 기반 모델은 부스팅 라운드를, 시계열 모델은 epoch를 기준으로 한다.

|  | LightGBM | XGBoost | LSTM / GRU |
| --- | --- | --- | --- |
| 학습률 | 0.05 | 0.05 | 0.001 (Adam, Weight Decay 1\mathrm{e}{-5}) |
| 구조 | Num Leaves 31, Min Data in Leaf 100 | Max Depth 6 | Hidden 64, 2층, FC 64–32–1, 입력 윈도우 14일 |
| 규제 | Bagging 0.9, Feature Fraction 0.9 | Subsample 0.9, Colsample 0.9 | Dropout 0.2 |
| 반복 | 최대 300 rounds | 최대 300 rounds | 최대 30 epochs, Batch 16,384 |
| Early Stopping | 20 rounds (Average Precision) | 20 rounds (AUC-PR) | 5 epochs (검증 손실) |

### 4.2 임곗값 선정 및 실험 구성

**임곗값 선정**

두 평가 방식은 각각의 기준으로 의사결정 임곗값을 선정한다. 검증 데이터에서 임곗값을 0.001 간격으로 탐색하되, 행 단위 평가에서는 행 단위 FAR이 1% 이하가 되는 최소 임곗값을 $\tau_{\mathrm{row}}$로, 운영 환경 기반 평가에서는 HDD-level FAR이 1% 이하가 되는 최소 임곗값을 $\tau_{\mathrm{op}}$로 선정하였다. 두 임곗값은 검증 데이터에서 독립적으로 선정한 뒤 동일한 테스트 데이터에 적용하였다.

검증 데이터에서 제약을 만족하는 임곗값이 탐색 범위 내에 존재하지 않는 경우에는 제약 위반이 가장 작은 임곗값을 사용하고 해당 seed도 집계에 포함하였다.

대규모 스토리지 시스템에서는 개별 HDD의 불필요한 Alarm도 시스템 규모에서는 상당한 운영 부담으로 누적되므로, 두 평가 방식 모두 오탐 수준을 제한하는 제약을 부여하였다. 두 평가 방식의 오탐 수준을 동일한 수치적 기준에서 비교하기 위해 행 단위 FAR과 HDD-level FAR에 모두 1%의 제약을 적용하였다. 다만 두 지표는 적용 모집단과 산출 방식이 다르므로, 동일한 1%가 동일한 운영적 엄격도를 의미하는 것은 아니다. 이러한 차이는 5.2절에서 분석한다.

**평가 대상 행 집합의 통제**

두 평가 방식은 동일한 온라인 추론 결과에 대해 동일한 행 집합을 사용한다. 운영 환경 기반 평가는 각 HDD의 전체 관측 이력을 시간 순으로 처리하는 것을 전제로 하므로, 행 단위 평가를 이와 다른 행 집합에서 산출할 경우 두 평가의 차이가 평가 절차의 차이에서 비롯된 것인지 평가 대상 데이터의 차이에서 비롯된 것인지 구분할 수 없다. 본 연구는 평가 절차가 성능 해석에 미치는 영향을 분리하여 관찰하는 것을 목적으로 하므로, 평가 대상 행 집합을 두 평가에서 동일하게 고정하였다.

다만 이러한 구성에서는 양성 레이블 비율이 데이터셋에 따라 0.083~0.181%로 낮고 음성 행의 비중이 매우 크므로, 본 연구에서 산출한 행 단위 Precision은 고장 전 일정 구간만을 평가에 사용하거나 HDD별로 표본을 추출하여 평가한 선행 연구의 보고값보다 낮게 나타날 수 있다. 본 연구의 비교는 행 단위 지표의 절대적 수준이 아니라, 동일한 예측 결과와 동일한 행 집합에서 평가 절차만을 달리했을 때 나타나는 상대적 차이에 근거하므로 이러한 수준의 차이는 5장의 해석에 영향을 주지 않는다.

**실험 구성**

이상의 설정에서 평가 방식과 임곗값 기준을 조합하여 표 5의 네 가지 분석을 수행하고, 이를 30개 random seed(42–71)에 대해 반복하였다. 5.1과 5.2는 각각 평가 방식과 임곗값 기준 중 하나만 변경하고 나머지를 고정하므로, 관찰되는 차이를 변경한 조건에 귀속시킬 수 있다. 5.3은 두 변경을 순차적으로 적용하여 세 조건에서의 모델 간 상대적 우열을 비교한다.

**표 4. 평가 방식과 임곗값 기준의 조합으로 구성한 네 가지 분석**

| 분석 | 평가 방식 | 임곗값 | 결과 |
| --- | --- | --- | --- |
| 평가 방식의 영향 | 행 단위 ↔ 운영 환경 기반 | $\tau_{\mathrm{row}}$ 고정 | 5.1 |
| 임곗값 기준의 영향 | 운영 환경 기반 고정 | $\tau_{\mathrm{row}}$ ↔ $\tau_{\mathrm{op}}$ | 5.2 |
| 모델 선택에 미치는 영향 | 행 단위 → 운영 환경 기반 | $\tau_{\mathrm{row}}$ → $\tau_{\mathrm{op}}$ | 5.3 |
| 운영 특성 분석 | 운영 환경 기반 고정 | $\tau_{\mathrm{op}}$ 고정 | 5.4 |

---

## 5. 실험 결과

본 장에서는 동일한 모델 예측 결과에 대해 평가 방식과 임곗값 최적화 기준을 달리 적용하여 성능을 비교한다. 5.1에서는 임곗값을 $\tau_{\mathrm{row}}$로 고정한 채 평가 방식만 행 단위 평가에서 운영 환경 기반 평가로 바꾸었을 때 나타나는 차이를 분석하고, 5.2에서는 평가 방식을 운영 환경 기반 평가로 고정한 채 임곗값 기준만 $\tau_{\mathrm{row}}$에서 $\tau_{\mathrm{op}}$로 바꾸었을 때의 운영 성능 변화를 분석한다. 5.3에서는 두 변경이 모델 간 상대적 우열과 모델 선택 결과에 미치는 영향을 30개 seed에 걸쳐 확인하며, 5.4에서는 운영 환경 기반 평가가 산출하는 시간적·운영적 정보를 분석한다.

이하에서 하나의 실험은 하나의 (데이터셋, 모델, seed) 조합을 가리키며, 모든 결과는 test split에서 산출하였다. 동일한 데이터셋–모델 조합에 대한 30개 seed의 결과는 중앙값으로 요약하고, 두 조건의 비교는 항상 동일한 실험 내에서 짝지어 수행한다. 30개 seed는 동일한 테스트 HDD 집합을 공유하는 재무작위화이므로 독립적인 표본으로 간주하지 않으며, 따라서 통계적 검정을 수행하는 대신 360개(30 seed × 12 데이터셋–모델 조합) 비교에서 관찰된 방향의 일관성을 함께 제시한다. 다만 5.4는 개별 HDD의 시간축 정보를 다루므로 HGST 데이터셋과 seed 42의 결과를 사례로 사용하며, 해당 절의 수치는 다른 절에서 제시하는 30개 seed 중앙값과 일치하지 않는다.

### 5.1   **행 단위 평가와 운영 환경 기반 평가**의 성능 비교

본 절에서는 동일한 모델 예측 결과와 동일한 임곗값에 두 평가를 각각 적용한 결과를 비교한다. 임곗값은 검증 데이터에서 행 단위 FAR 1% 이하를 만족하는 최소 임곗값 $\tau_{\mathrm{row}}$를 사용하였다. 표 1의 세 번째 열은 운영 환경 기반 평가를 기준으로 재선정한 $\tau_{\mathrm{op}}$의 결과이며, 임곗값 기준에 따른 차이는 5.2절에서 분석한다.

**표 5. 평가 방식과 임곗값 기준에 따른 성능 비교 (30개 seed 중앙값, test split).** 각 칸은 Precision / Recall / FAR 순이며, HDD-level 열에는 Median Lead Time(일)을 함께 표기하였다. 앞의 두 결과 열은 동일한 임곗값 $\tau_{\mathrm{row}}$에서 평가 방식만 달리한 결과이고, 세 번째 결과 열은 운영 환경 기반 평가를 기준으로 재선정한 $\tau_{\mathrm{op}}$의 결과이다.

| Dataset | Model | Row-level$(\tau_{\mathrm{row}})$ Prec / Rec / FAR | HDD-level$(\tau_{\mathrm{row}})$ Prec / Rec / FAR / Med. LT | HDD-level$(\tau_{\mathrm{op}})$ Prec / Rec / FAR / Med. LT |
| --- | --- | --- | --- | --- |
| HGST | LGBM | 0.075 / 0.440 / 0.96% | 0.089 / 0.105 / 7.47% / 221 | 0.302 / 0.086 / 1.13% / 56 |
|  | XGB | 0.081 / 0.469 / 0.95% | 0.071 / 0.086 / 7.93% / 229 | 0.263 / 0.068 / 1.13% / 64 |
|  | LSTM | 0.070 / 0.453 / 1.09% | 0.066 / 0.068 / 6.14% / 223 | 0.266 / 0.117 / 1.54% / 80 |
|  | GRU | 0.071 / 0.459 / 1.07% | 0.057 / 0.062 / 6.96% / 223 | 0.283 / 0.136 / 1.74% / 72 |
| ST12000NM0007 | LGBM | 0.067 / 0.370 / 0.97% | 0.252 / 0.302 / 3.53% / 32 | 0.439 / 0.236 / 1.14% / 11 |
|  | XGB | 0.065 / 0.391 / 1.05% | 0.209 / 0.300 / 4.76% / 38 | 0.419 / 0.249 / 1.15% / 14 |
|  | LSTM | 0.065 / 0.379 / 1.02% | 0.167 / 0.273 / 6.02% / 47 | 0.354 / 0.182 / 1.08% / 24 |
|  | GRU | 0.070 / 0.386 / 0.97% | 0.171 / 0.284 / 6.18% / 45 | 0.412 / 0.224 / 1.04% / 21 |
| TOSHIBA | LGBM | 0.084 / 0.392 / 0.36% | 0.193 / 0.196 / 2.33% / 68 | 0.306 / 0.148 / 0.55% / 45 |
|  | XGB | 0.044 / 0.462 / 0.88% | 0.160 / 0.220 / 4.04% / 76 | 0.282 / 0.163 / 0.89% / 46 |
|  | LSTM | 0.039 / 0.478 / 1.03% | 0.082 / 0.177 / 8.25% / 112 | 0.275 / 0.134 / 0.79% / 52 |
|  | GRU | 0.038 / 0.478 / 1.03% | 0.077 / 0.177 / 8.77% / 113 | 0.287 / 0.148 / 0.86% / 48 |

임곗값을 $\tau_{\mathrm{row}}$로 고정한 채 평가 방식만 바꾸면 세 지표가 서로 다른 방향으로 이동한다. 즉 운영 환경 기반 평가의 결과는 행 단위 결과를 일정한 비율로 환산하거나 재집계하여 얻을 수 없다. FAR은 행 단위에서 0.36~1.09%로 유지되는 동안 HDD-level에서는 2.33~8.77%로 나타났고 Recall은 HDD-level 값이 더 낮았으며(HGST의 LightGBM 0.440 → 0.105), 두 방향 모두 360개 짝지은 비교 전부에서 성립하였다. 반면 Precision은 방향이 일정하지 않아 294개(82%)에서 HDD-level 값이 더 높았으나 12개 조합 중 8개에서만 방향이 일관되었고, HGST에서는 네 모델 중 셋에서 오히려 낮아졌다.

세 지표의 차이는 서로 다른 이유에서 발생한다. 행 단위 FAR은 개별 행의 오탐 비율이므로 동일 HDD에서 반복된 Alarm이 운영 부담으로 누적되는 정도를 나타내지 못하는 반면, HDD-level FAR은 실제로 몇 대가 점검 대상이 되는지를 직접 나타낸다. Recall의 두 값은 성능의 높낮이가 아니라 서로 다른 대상을 측정한 결과다. HGST의 LightGBM에서는 고장이 관측된 162대 중 Alarm이 발생한 HDD가 116대에 이르렀음에도 On-time은 17대에 그쳤다(Early 99대, Missed 46대).

모델 간 차이의 크기 역시 평가 방식에 따라 달라졌다. HGST에서 행 단위 Recall의 최대·최소 비는 1.07배였으나 동일한 임곗값의 HDD-level Recall에서는 1.70배였다. 다만 ST12000NM0007(1.06배 대 1.11배)과 TOSHIBA(1.22배 대 1.24배)에서는 차이가 크지 않아, 격차의 확대 정도는 데이터셋에 따라 달랐다.

두 평가의 값이 다르다는 사실보다 중요한 것은 어느 쪽이 운영 의사결정에 필요한 정보인가이다. 유지보수 계획은 개별 관측 행이 아니라 HDD를 대상으로 수립되므로, 운영자가 알아야 하는 것은 오탐 행의 비율이 아니라 실제로 몇 대가 점검 대상이 되고 그 경보가 대응 가능한 시점에 발생했는지이다. 행 단위 FAR 1%는 이 질문에 답하지 못하며, 우측 중도절단 HDD의 약 2~9%에서 최초 Alarm이 발생한다는 사실과 그 이후 확보되는 대응 시간은 운영 환경 기반 평가를 통해서만 확인된다.

### 5.2 운영 환경 기준 임곗값 최적화 결과

5.1절에서 동일한 $\tau_{\mathrm{row}}$를 사용하더라도 두 평가의 결과가 크게 달라지는 것을 확인하였다. 이에 본 절에서는 운영 환경 기반 평가를 기준으로 임곗값을 다시 선정하는 것이 운영 성능에 어떠한 영향을 미치는지 분석하였다.

두 기준이 선택한 임곗값 자체가 크게 달랐다. $\tau_{\mathrm{op}}$는  $\tau_{\mathrm{row}}$의 3.9배에서 127.5배에 이르렀으며(조합별 중앙값), 이는 본 실험 조건에서 행 단위 FAR 1% 제약으로 선정한 임곗값이 HDD-level FAR을 동일한 수준으로 제한하지 못하였음을 의미한다.

**표 6. 임곗값 최적화 기준에 따른 운영 성능 변화**

| 지표 | $\tau_{\mathrm{row}}$ | $\tau_{\mathrm{op}}$ | 같은 방향 조합 |
| --- | --- | --- | --- |
| HDD-level Precision | 0.124 | 0.295 | 12/12 |
| HDD-level Recall | 0.187 | 0.148 | 10/12 |
| HDD-level FAR | 6.16% | 1.10% | 12/12 |
| Median Lead Time | 93.5일 | 46.8일 | 12/12 |

각 값은 데이터셋–모델 조합마다 30개 seed의 중앙값을 구한 뒤 12개 조합에 대해 다시 중앙값을 취한 것이며, 마지막 열은 그 이동과 같은 방향으로 변한 조합의 수이다.

임곗값 기준을 $\tau_{\mathrm{op}}$로 바꾸면 오탐 부담이 크게 줄고 발생한 Alarm의 적시성이 높아진다(표 6). 이 두 방향과 Median Lead Time의 감소는 360개 비교 전부에서 예외 없이 나타났다. 다만 Median Lead Time의 감소는 360개 비교 전부에서 예외 없이 나타났다. 다만 Median Lead Time은 단조적인 성능 지표가 아니므로 이 감소는 성능 향상이 아니라 Alarm 시점의 운영적 특성이 변화한 결과이며, 임곗값 상승에 따른 개별 HDD의 Lead Time 변화와 최초 Alarm이 사라진 HDD가 산출 모집단에서 제외된 효과가 함께 반영된 것이다.

반면 HDD-level Recall은 임곗값에 대해 단조적이지 않다. 중앙값은 0.187에서 0.148로 낮아졌으나 12개 조합 중 2개에서는 임곗값을 높였음에도 상승하였고, HGST의 LSTM(0.068→0.117)과 GRU(0.062→0.136)는 30개 seed 전부에서 상승하였다. 임곗값이 오르면 최초 Alarm이 뒤로 밀리면서 Early로 분류되던 HDD가 Prediction Horizon 이내로 들어와 On-time이 될 수 있기 때문이다. 이는 두 평가가 동일한 예측 결과를 서로 다른 방식으로 판정하기 때문에 나타나는 구조적 차이로, 운영 성능을 행 단위 결과로부터 유추할 수 없다는 3장의 논의를 뒷받침한다.

$\tau_{\mathrm{op}}$는 검증 데이터에서 제약을 만족하도록 선정한 뒤 테스트 데이터에 적용한 값이므로 테스트 FAR은 제약선을 초과할 수 있다. 검증에서는 모든 조합의 FAR 중앙값이 0.70~0.99%로 제약을 만족하였으나, 테스트에서는 12개 조합 중 8개가 1%를 상회하였다(1.04~1.74%). 이는 HDD-level FAR이 소수의 중도절단 HDD 사건으로 추정되는 데 따른 변동으로, HGST에서 $\tau_{\mathrm{op}}$ 적용 시 Alarm이 발생한 중도절단 HDD는 977대 중 중앙값 기준 11~17대에 불과하다. 또한 임곗값 탐색 범위의 경계에서 비롯된 현상도 관찰되어, HGST의 LightGBM은 30개 seed 중 23개에서만 제약을 만족하였고†† TOSHIBA의 LightGBM에서는 $\tau_{\mathrm{row}}$가 탐색 범위 하단에 위치하여 행 단위 FAR이 다른 모델보다 낮게 나타났다. 따라서 임곗값 기반 평가에서는 예측확률의 분포와 탐색 범위 설정을 함께 고려할 필요가 있다.

†† LightGBM의 예측확률이 정확히 1.0에 포화되는 HDD가 존재하여, 탐색 범위의 상한(0.999)으로는 해당 HDD의 Alarm을 억제할 수 없었다.

마지막으로 두 임곗값 기준은 동일하게 1%를 사용하지만 적용 모집단이 다르므로 실질적인 엄격도가 같지 않다. 행 단위 FAR 1%에 해당하는 임곗값에서 이미 상당 비율의 우측 중도절단 HDD에 최초 Alarm이 발생하므로(5.1절), $\tau_{\mathrm{op}}$는 $\tau_{\mathrm{row}}$보다 실질적으로 훨씬 보수적인 제약이다. 따라서 본 절의 결과는 두 기준의 엄격도를 통제한 비교가 아니라, 각 평가 방식이 자신의 기준에서 동일한 수치적 제약을 적용했을 때 나타나는 차이로 해석해야 한다. 엄격도를 일치시킨 비교는 향후 과제로 남긴다.

### 5.3 평가 조건에 따른 모델 선택의 변화

앞선 결과가 특정 학습 seed에서만 나타나는 현상인지, 그리고 평가 조건의 차이가 모델 선택 결과까지 바꾸는지를 확인하기 위해 30개 random seed에 대해 동일한 학습 및 평가 절차를 반복하였다.

모델 간 상대적 성능 우위는 각 seed에서 Recall이 가장 높은 모델을 1위로 판정하여 비교하였다. 동일한 seed는 동일한 학습 모델과 동일한 test HDD 집합을 공유하므로, 두 조건의 차이는 seed 내에서 짝지어진 대조에 해당한다. 다만 seed는 동일한 test 집합을 공유하는 재무작위화이므로 통계적 검정은 수행하지 않고, 반복 실험에서 관찰된 일관성만을 제시한다.

**표 7. 평가 조건에 따른 1위 모델의 일치율 (30개 seed).** 각 seed에서 Recall이 가장 높은 모델을 1위로 판정하고, 두 조건에서 1위 모델이 일치한 seed의 비율을 나타낸다. 동점인 경우 공동 1위 모델의 집합이 완전히 일치할 때만 일치로 집계하였다. 마지막 열은 평가 조건을 고정한 채 서로 다른 두 seed 사이의 1위 모델이 일치한 비율로, 학습의 무작위성만으로 발생하는 변동 수준을 나타낸다.

| 데이터셋 | 평가 방식만 변경 | 임곗값 기준만 변경 | 두 평가를 각자 기준으로 | (대조) 동일 조건 seed 간 |
| --- | --- | --- | --- | --- |
| HGST | 3% | 0% | 27% | 50% |
| ST12000NM0007 | 37% | 63% | 40% | 45% |
| TOSHIBA | 13% | 53% | 13% | 47% |

**평가 방식만 바꾼 경우 1위 모델은 대부분의 seed에서 달라졌다.** HGST에서는 30개 seed 중 한 개(3%)에서만 1위가 유지되었고 TOSHIBA에서도 13%에 그쳤다. 이동 방향 또한 일관되어, LightGBM은 행 단위 평가에서 세 데이터셋 모두 최하위였으나 운영 환경 기반 평가에서는 모두 상위권으로 올라섰고 GRU는 반대 방향으로 이동하였다. 모델 쌍 단위로 보면 18개 쌍 가운데 9개에서 우열의 다수 방향이 뒤바뀌었으며, 특히 TOSHIBA의 LightGBM은 LSTM 및 GRU에 대해 행 단위 평가에서 30개 seed 전부 열세였으나 운영 환경 기반 평가에서는 30개 seed 전부 우세로 나타나 어느 seed에서도 예외가 없었다. 두 평가에 동일한 예측값과 동일한 임곗값을 사용하였으므로, 이 변화는 두 평가 절차가 동일한 예측 결과로부터 성능을 판정하는 방식의 차이에 귀속된다.

**임곗값 기준만 바꾼 경우의 영향은 데이터셋에 따라 갈렸다.** HGST에서는 30개 seed 전부에서 1위가 바뀌었으며(일치율 0%), $\tau_{\mathrm{row}}$ 기준으로 26개 seed에서 1위였던 LightGBM이 $\tau_{\mathrm{op}}$에서는 GRU 22개, LSTM 5개, 두 모델 공동 1위 3개로 나타나 트리 기반에서 순환신경망 기반으로 1위 모델군이 전환되었다. 반면 ST12000NM0007과 TOSHIBA에서는 절반 이상의 seed에서 1위가 유지되어(63%, 53%) 변화가 제한적이었다.

이러한 변화가 학습의 무작위성만으로 설명되지 않는다는 점은 표 7의 마지막 열과의 대조에서 확인된다. 평가 조건을 고정한 채 seed만 달리했을 때에도 1위 모델은 45~50%에서만 일치하여, 반복 학습만으로도 인접한 모델 간 순위는 흔들린다. 그럼에도 평가 방식을 바꾼 경우의 일치율은 3~37%로 이보다 낮았으며, 특히 HGST에서는 seed 간 50%인 일치율이 평가 방식 변경 시 3%까지 떨어졌다. 다만 ST12000NM0007과 TOSHIBA에서 임곗값 기준만 바꾼 경우의 일치율(63%, 53%)은 seed 간 일치율보다 높아, 이 두 데이터셋에서는 임곗값 기준의 영향이 학습 변동을 넘어서지 않았다.

**해석상의 한계**

본 절의 결과는 비교 대상 모델이 4개로 제한된 조건에서 얻은 것이며, seed 역시 동일한 test HDD 집합을 공유한다. 따라서 관찰된 역전의 빈도를 모집단 수준의 일반적 현상으로 확대 해석하기보다는, **동일한 예측 결과에 어떤 평가 절차와 임곗값 기준을 적용하는지가 모델 선택 결과를 바꿀 수 있음을 본 실험 조건에서 반복적으로 확인한 결과**로 한정하여 해석한다. 그러나 TOSHIBA에서 관찰된 0/30 → 30/30의 결과는 30개 seed 전부에서 동일한 방향으로 나타났으므로, 이러한 역전이 학습의 무작위성만으로 발생한 결과라고 보기는 어렵다.

### 5.4 운영 환경 기반 평가를 통한 HDD별 운영 특성 분석

운영 환경 기반 평가는 HDD별 시간 순서에 따른 Alarm 발생 정보를 유지하면서 최초 Alarm을 기준으로 HDD-level 결과를 산출한다. 따라서 행 단위 또는 집계형 성능지표만으로는 확인하기 어려운 HDD별 적시 탐지의 안정성, 최초 Alarm의 발생 시점, 반복 Alarm과 False Alarm의 시간적 위치, 그리고 개별 HDD에서의 예측확률 변화를 추가적으로 분석할 수 있다.

#### 5.4.1 HDD별 On-time 탐지 안정성

!**그림 N. HDD별 On-time 탐지 횟수 분포 (HGST HUH721212ALN604, 30개 학습 seed, $\tau_{\mathrm{op}}$ 기준).** 고장이 관측된 162대의 HDD 각각에 대해 30개 seed 중 On-time으로 판정된 횟수를 집계하였다. 0회 막대의 회색 부분은 30개 seed 모두에서 Alarm이 발생하지 않은 HDD를 나타낸다. 색상으로 표시된 0회 구간은 일부 seed에서 Alarm이 발생했으나 발생한 Alarm이 모두 Early로 판정되어 On-time으로 인정되지 않은 HDD를 나타내며, 이 집단에는 일부 seed에서 Missed로 판정된 HDD도 포함된다. 각 패널 제목의 비율은 30개 seed 중 한 번도 On-time으로 탐지되지 않은 HDD의 비율이다.

**그림 N. HDD별 On-time 탐지 횟수 분포 (HGST HUH721212ALN604, 30개 학습 seed, $\tau_{\mathrm{op}}$ 기준).** 고장이 관측된 162대의 HDD 각각에 대해 30개 seed 중 On-time으로 판정된 횟수를 집계하였다. 0회 막대의 회색 부분은 30개 seed 모두에서 Alarm이 발생하지 않은 HDD를 나타낸다. 색상으로 표시된 0회 구간은 일부 seed에서 Alarm이 발생했으나 발생한 Alarm이 모두 Early로 판정되어 On-time으로 인정되지 않은 HDD를 나타내며, 이 집단에는 일부 seed에서 Missed로 판정된 HDD도 포함된다. 각 패널 제목의 비율은 30개 seed 중 한 번도 On-time으로 탐지되지 않은 HDD의 비율이다.

운영 환경 기반 평가는 HDD별 최초 Alarm 판정 결과를 보존하므로, 동일한 HDD가 반복 학습 결과에서 어떤 운영 판정을 받는지 추적할 수 있다. 그림 N은 고장이 관측된 162대의 HDD가 30개 seed 중 몇 번 On-time으로 탐지되었는지를 모델별로 나타낸 것이다. 네 모델에서 한 번도 On-time으로 탐지되지 않은 HDD의 비율은 67~73%에 이르렀으며, 이는 최초 Alarm이 전혀 발생하지 않은 경우와 일부 seed에서 Alarm이 발생했지만 발생한 Alarm이 모두 Prediction Horizon 밖에 위치한 경우로 나뉜다. 네 모델과 30개 seed로 구성된 120개 모델–seed 조합 전체에서도 87대(54%)의 HDD가 한 번도 On-time으로 탐지되지 않았고, 그중 57대(35%)는 어떤 조합에서도 Alarm 자체가 발생하지 않았다.

적시 탐지 이력이 있는 HDD에서도 탐지는 안정적이지 않았다. 한 번 이상 On-time으로 탐지된 HDD의 탐지 횟수 중앙값은 LightGBM, XGBoost 및 LSTM에서 30회 중 5회였다. 트리 기반 두 모델에서는 30개 seed 전부에서 On-time으로 탐지된 HDD가 하나도 없었고 LSTM에서도 3대에 그쳤다. 반면 GRU에서는 26회 이상 On-time으로 탐지된 HDD가 15대로 나타나, 해당 실험 조건에서 적시 탐지가 반복적으로 관찰된 HDD 집합이 상대적으로 안정적인 양상을 보였다.

행 단위 집계 지표만으로는 동일한 HDD가 반복적으로 놓쳐지는지, 아니면 매번 다른 HDD가 놓쳐지는지를 구분하기 어렵다. 이 결과는 낮은 HDD-level Recall이 평균적인 성능 부족만으로 설명되지 않으며, 특정 운영점에서 반복적으로 Missed로 판정되거나 Alarm이 발생하더라도 Early로만 판정되는 HDD 집단이 존재함을 보여준다.

#### **5.4.2 최초 Alarm의 Lead Time 분포**

!**그림 3.** 모델별 최초 Alarm의 Lead Time 분포(HGST HUH721212ALN604, seed 42, \tau_{\mathrm{op}} 기준). 고장이 관측된 HDD 중 최초 Alarm이 발생한 HDD를 대상으로 하며, 각 패널의 n은 해당 HDD 수(On-time + Early)이다. 세로축은 상대빈도(%), 적색 파선은 중앙값, 회색 점선은 H=30일이다. 가로축은 10일 구간이며 360일을 초과하는 표본은 축 우측 끝의 빗금 막대에 합산하였고, 중앙값은 축 범위 밖 표본을 포함하여 산출하였다.

**그림 3.** 모델별 최초 Alarm의 Lead Time 분포(HGST HUH721212ALN604, seed 42, \tau_{\mathrm{op}} 기준). 고장이 관측된 HDD 중 최초 Alarm이 발생한 HDD를 대상으로 하며, 각 패널의 n은 해당 HDD 수(On-time + Early)이다. 세로축은 상대빈도(%), 적색 파선은 중앙값, 회색 점선은 H=30일이다. 가로축은 10일 구간이며 360일을 초과하는 표본은 축 우측 끝의 빗금 막대에 합산하였고, 중앙값은 축 범위 밖 표본을 포함하여 산출하였다.

최초 Alarm의 Lead Time 중앙값은 LightGBM 105.0일, XGBoost 57.0일, LSTM 77.0일, GRU 75.0일로 네 모델 모두 Prediction Horizon(30일)을 크게 상회하였다. 최초 Alarm이 발생한 고장 관측 HDD 중 On-time으로 분류된 비율은 27~~39%에 그쳐, 나머지 61~~73%는 고장 이전에 Alarm이 발생하였음에도 Prediction Horizon 밖이어서 Early로 분류되었다. 즉 낮은 HDD-level Recall은 고장을 탐지하지 못한 경우뿐 아니라 최초 Alarm이 지나치게 이른 경우에서도 발생하며, 행 단위 평가는 각 행의 양성 예측 여부만을 집계하므로 두 경우를 구분하지 못한다.

모델 간 차이도 확인된다. 최초 Alarm이 발생한 고장 관측 HDD는 LSTM 65대, GRU 63대로 LightGBM 37대, XGBoost 36대의 약 1.8배였으며, 이는 5.3절에서 \tau_{\mathrm{op}} 기준 1위 모델이 순환신경망 계열로 전환된 현상과 같은 방향의 관찰이다. 분포의 폭 또한 달라, Lead Time이 360일을 초과한 사례가 XGBoost는 2.8%인 반면 LightGBM은 16.2%로 최초 Alarm이 발생한 HDD 6대 중 약 1대에서 고장 1년 이상 전에 첫 경보가 울렸다.

본 절의 수치는 seed 42의 결과이므로 30개 seed 중앙값인 표 1의 Median Lead Time과 다르며, 특히 LightGBM은 seed에 따른 \tau_{\mathrm{op}}의 변동이 커 차이가 크게 나타난다.

#### **5.4.3 반복 Alarm 및 False Alarm의 시간적 발생 특성**

고장이 관측된 HDD에서 발생한 전체 Alarm은 LightGBM 915건, XGBoost 1,505건, GRU 2,828건, LSTM 4,607건으로, 최초 Alarm이 발생한 HDD 한 대당 각각 약 25건, 42건, 45건, 71건에 해당한다. 즉 동일한 최초 Alarm 하나에 대해 이후 누적되는 경보 부담이 모델에 따라 약 3배까지 달랐다. 시간적 분포에서도 차이가 나타나, XGBoost는 Alarm이 고장 이전 1년 이내에 거의 모두 분포한 반면(360일 초과 0.1%) LSTM과 GRU는 각각 13.3%, 11.0%가 1년을 넘는 시점에 발생하였다. 특히 LSTM은 최초 Alarm의 Lead Time 중앙값이 77일인 데 비해 전체 Alarm의 중앙값은 117일로, 경보가 고장에서 먼 구간에 오래 누적되는 양상을 보였다. 행 단위 Recall은 반복 예측을 개별 사건으로 합산하므로 그 값이 몇 대의 HDD에서 몇 번 반복된 결과인지 구분하지 못하는 반면, 최초 Alarm의 Lead Time과 전체 Alarm의 통계는 첫 경보의 시점과 이후의 반복 양상을 보완적으로 나타낸다.

False Alarm의 최초 발생 시점 또한 보존된다. 본 실험에서는 네 모델 모두 관측 후반부에 집중되어, 관측 시작 후 약 1,400일 이전에 발생한 오탐이 모델별로 0~3건에 불과하였다. 이는 동일한 FAR 수준에서도 오탐이 운영 기간 중 언제 발생하는지가 모델에 따라 다를 수 있음을 보여준다. 다만 평가 구간 내 최초 Alarm이 발생한 중도절단 HDD의 수는 HDD-level FAR을 1% 이하로 제한한 결과 977대 가운데 10대 안팎으로 정해지며, 여기에 관측일수의 산포가 좁다는 점과 시점별 위험집합의 크기를 함께 고려해야 하므로 본 분석은 경향의 확인에 한정한다.

#### **5.4.4 운영 타임라인 사례 분석**

!**그림 5.** 운영 환경 기반 평가의 네 가지 판정 사례에 대한 운영 타임라인(HGST HUH721212ALN604, GRU, seed 42, $\tau_{\mathrm{op}}$ = 0.149). 각 패널은 해당 판정으로 분류된 대표 HDD 한 대의 관측 종료 직전 1년 구간을 나타낸다. 청색 실선은 예측확률, 적색 파선은 의사결정 임곗값, 적색 점선은 최초 Alarm 시점, 흑색 파선은 실제 고장 시점이며, 음영은 예측확률이 임곗값을 상회한 구간이다. (a) On-time, (b) Early, (c) Censored Early, (d) Missed.

**그림 5.** 운영 환경 기반 평가의 네 가지 판정 사례에 대한 운영 타임라인(HGST HUH721212ALN604, GRU, seed 42, $\tau_{\mathrm{op}}$ = 0.149). 각 패널은 해당 판정으로 분류된 대표 HDD 한 대의 관측 종료 직전 1년 구간을 나타낸다. 청색 실선은 예측확률, 적색 파선은 의사결정 임곗값, 적색 점선은 최초 Alarm 시점, 흑색 파선은 실제 고장 시점이며, 음영은 예측확률이 임곗값을 상회한 구간이다. (a) On-time, (b) Early, (c) Censored Early, (d) Missed.

본 그림은 3장에서 정의한 다섯 개 결과 범주 중 **Alarm 또는 고장이라는 시간축상의 사건을 갖는 네 가지 사례를 제시한다.** Censored No Alarm은 최초 Alarm과 관측된 고장이 모두 존재하지 않아 시간축에 표시할 사건이 없으므로 제외하였다.

On-time 사례(a)에서는 예측확률이 장기간 낮게 유지되다가 고장 직전 급상승하여 고장 16일 전에 최초 Alarm이 발생하고, 이후 고장 시점까지 임곗값을 계속 상회한다. 반면 Censored Early 사례(c)에서는 예측확률이 일시적으로 임곗값을 초과한 뒤 곧 하락하여 관측 종료까지 낮은 수준을 유지하며, Missed 사례(d)에서는 고장에 가까워지며 예측확률이 완만히 상승하고 일부 구간에서 임곗값에 근접하지만 끝내 이를 넘지 않는다. 미탐지 사례에서도 예측확률이 어느 수준까지 상승했는지, 즉 임곗값을 조금만 낮추었을 때 탐지 가능성이 있었는지를 함께 확인할 수 있다.

Early 사례(b)는 최초 Alarm이 고장 332일 전에 발생한 뒤 예측확률이 임곗값 부근에서 하락과 재상승을 반복하여 약 250일에 걸쳐 임곗값을 6회 다시 상회하지만, 정작 고장에 근접한 구간에서는 임곗값 아래로 내려간다. 이는 지나치게 이른 최초 Alarm이 장기간의 경보 부담을 발생시키면서도 적시 탐지로 이어지지 않을 수 있음을 보여주며, Early를 On-time과 구분하여 평가할 필요성을 시간축에서 뒷받침한다.

종합하면 운영 타임라인은 동일한 판정 범주에 속하는 HDD에 대해서도 최초 임곗값 초과 시점, 고장과의 시간적 관계, Alarm의 지속·반복 양상, 미탐지 과정의 예측확률 변화가 서로 다르게 나타남을 개별 HDD 수준에서 보여준다. 또한 최고 예측확률이 On-time 0.67, Early 0.31, Censored Early 0.26으로 나타나 판정 결과뿐 아니라 위험 신호의 강도에서도 차이가 확인된다.

이상의 분석을 통해 운영 환경 기반 성능평가는 집계형 성능지표만으로는 확인하기 어려운 다섯 가지 운영 정보를 제공함을 확인하였다. HDD별 On-time 탐지 횟수는 어떤 HDD가 반복적으로 적시 탐지되고 어떤 HDD가 반복적으로 놓쳐지는지를, 최초 Alarm의 Lead Time은 고장 이전 언제부터 해당 HDD가 위험 대상으로 식별되기 시작하는지를, 전체 Alarm의 시간적 분포는 최초 Alarm 이후 경보가 어느 기간에 걸쳐 반복되는지를, False Alarm의 발생 시점은 오탐이 운영 기간 중 언제 발생하는지를, 개별 HDD의 운영 타임라인은 예측확률의 변화와 임곗값 초과 및 Alarm의 반복·지속 양상을 시간축에서 보여준다. 즉 운영 환경 기반 평가는 탐지 여부나 집계된 성능 수준을 넘어, 모델의 경보가 언제 발생하고 고장까지 얼마나 앞서 나타나며 이후 어떻게 반복·변화하는지를 확인할 수 있는 시간적·운영적 정보를 제공한다.

나아가 이러한 정보는 운영 의사결정의 근거로 활용될 수 있다. 예컨대 최초 Alarm 이후 누적되는 반복 경보가 HDD당 약 25~71건에 이른다는 사실은 개별 Alarm을 그대로 정비 지시로 연결하기보다 지속성 조건 등 별도의 경보 정책이 필요함을 시사하며, 오탐이 관측 후반부에 집중된다는 사실은 HDD의 누적 운영 기간에 따라 임곗값이나 점검 주기를 달리 적용하는 방안을 검토할 근거가 된다.

---

## 6. 결론

본 연구는 HDD 고장 예측 모델의 성능을 개별 관측 행의 분류 정확도만으로 평가할 경우, HDD 단위의 경보 과정에서 나타나는 성능을 충분히 반영하기 어렵다는 문제의식에서 출발하였다. 이에 각 HDD의 SMART 관측 이력을 시간 순으로 처리하여 온라인 추론을 수행하고, 예측확률을 Alarm으로 변환한 뒤 최초 Alarm과 실제 고장 시점 또는 관측 종료 시점의 관계를 이용하여 각 HDD를 On-time, Early, Missed, Censored Early, Censored No Alarm으로 판정하는 운영 환경 기반 평가 방법을 구성하였다. 이 평가는 모델의 예측이 HDD 단위 경보로 전환되는 과정을 대상으로 하며, 경보 이후의 대응 정책은 포함하지 않는다. 3개 데이터셋과 4개 모델, 30개 random seed에 대해 동일한 예측 결과에 두 평가 절차와 두 임곗값 최적화 기준을 적용하여, 평가 기준에 따라 성능 측정 결과와 모델 선택이 어떻게 달라지는지를 분석하였다.

**연구 요약**

동일한 예측 결과와 동일한 임곗값을 사용하더라도 두 평가는 서로 다른 성능을 나타냈다. 행 단위 FAR이 약 1%로 유지되는 조건에서 HDD-level FAR은 조합별 중앙값 2.33~8.77%였고, Recall도 크게 달라져 HGST의 LightGBM은 행 단위 0.440이 HDD-level에서 0.105로 나타났다. 임곗값을 어떤 기준으로 최적화하는지에 따라서도 운영 성능이 달라져, HDD-level FAR을 기준으로 재선정하면 오탐 부담이 줄고 발생한 Alarm의 적시성이 높아지는 반면 Recall은 대체로 감소하였다. 이러한 방향은 360개 실험 전부에서 일관되게 나타났다.

평가 조건의 차이는 모델 선택 결과까지 바꾸었다. 평가 방식만 바꾸었을 때 18개 모델 쌍 중 9개에서 우열의 다수 방향이 뒤바뀌었고, 임곗값 기준만 바꾸었을 때 HGST에서는 30개 seed 전부에서 1위 모델이 트리 기반에서 순환신경망 기반으로 전환되었다. 각 평가 방식을 그 방식의 기준으로 적용하였을 때 1위 모델이 일치한 비율은 데이터셋에 따라 13~40%에 그쳤다. 또한 운영 환경 기반 평가는 집계된 성능지표 외에 HDD별 적시 탐지의 안정성, 최초 Alarm의 Lead Time, 반복 Alarm과 False Alarm의 시간적 위치, 개별 HDD의 예측확률 변화라는 운영 정보를 함께 제공하였다.

**연구 의의 및 시사점**

- 본 연구의 의의는 성능평가의 대상을 개별 관측 행의 분류 정확성에서 HDD의 시간적 운영 과정에서의 경보 성능으로 전환하고, 우측 중도절단을 포함한 5개 결과 범주를 기준으로 각 지표의 모집단을 명시적으로 정의하였다는 점에 있다. 제안한 지표는 5개 범주를 모두 사용하므로 어떤 지표에도 반영되지 않는 HDD는 존재하지 않는다.
- 또한 평가 기준은 성능 측정 단계뿐만 아니라 임곗값과 모델의 선택 결과에도 영향을 주었다. 따라서 운영에 사용할 모델과 임곗값을 결정할 때에는 행 단위 평가 결과를 그대로 적용하기보다, 언제 위험을 감지하고 고장까지 어느 정도의 시간적 여유가 확보되며 오탐이 언제 발생하는지를 함께 제시하는 운영 관점의 평가를 병행할 필요가 있다.

**연구의 한계 및 향후 연구**

- (데이터 범위) Backblaze의 3개 HDD 모델과 4개 예측 모델을 대상으로 하였으므로 모든 HDD 및 타 설비의 예지보전 문제로 일반화하기에는 한계가 있다.
- (평가 대상 설정) 본 연구는 학습에 사용되지 않은 HDD에 대한 일반화 성능을 평가 대상으로 삼아 HDD 단위 그룹 분할을 적용하였으며, 이 구성에서 학습과 테스트는 동일한 달력 구간을 공유한다. 수집 기간에 따른 고장률 변화가 성능 수준에 반영될 수 있으나 이 요인은 두 평가에 동일하게 작용하므로, 향후 시간 분할 조건에서의 검증이 필요하다.
- (운영 조건 설정) FAR 1% 제약과 Prediction Horizon 30일은 비교를 위한 설정이며, On-time/Early 경계와 주요 지표가 H에 의존하므로 다양한 H와 FAR 제약, 유지보수 비용 하에서의 민감도 분석이 필요하다.
- (중도절단 가정) 우측 중도절단이 고장 위험과 독립이라고 가정하였으며, 드라이브가 고장 외 사유로 운영에서 제외되는 과정이 열화와 상관되어 있다면 HDD-level FAR은 과대추정될 수 있다.
- (FAR 추정의 정밀도) HDD-level FAR은 소수의 중도절단 HDD 사건으로 추정되어 seed에 따른 임곗값 변동에 민감하다. 다중 seed 집계와 원 카운트 병기로 이를 완화하였으나 신뢰구간 기반 보고 등의 보완이 필요하다.
- (행 단위 비교 기준) 행 단위 평가에는 Sliding Window 등 다양한 절차가 존재하므로 비교 기준 설정의 차이가 결과에 미치는 영향을 추가 검증할 필요가 있다.
- (동적 운영 환경) 지속적 데이터 유입과 모델 재학습·갱신은 고려하지 않았으며, 온라인 학습을 포함한 동적 환경으로의 확장이 필요하다.
- (Alarm 대응 정책) 본 평가는 예측이 HDD 단위 경보로 전환되는 과정까지를 대상으로 하며, 경보 억제, 최소 지속시간 및 재경보 대기, 경보 단계화, 유지보수 비용·인력 제약은 반영하지 않았다. 최초 Alarm 이후 HDD당 약 25~71건의 반복 경보가 누적되므로, 이러한 정책 요소를 포함한 평가 체계로의 확장이 필요하다.